# V25 private directional rupture trial — ready for native inspection

Frozen patch: `v25-directional-rupture.patch`, 26,718 bytes, SHA-256 **04dd73428ddd613a64f0510bcd67431fa862c32f4a5f55a2a8cb02e8210a8ed8**. `git apply --check` passed against the preserved live inputs. No live files were changed by this preparation. Root owns integration/build/native capture.

The actual V24b standard critic remains **5/10 FAIL8**. Its source-PTS review and exact seven-image audit are under `output/playwright/round35-combat-v24b/`. The supplemental219.216887m/s video also has a clean camera return; it does not replace that critic. Target17 is a generated paintover for silhouette/local-contrast guidance only; the original critic reference/protocol stays unchanged.

The narrow cause addressed here is visible in the retained clips: both initial flash and sustained hot face use a centered radial shape; metal departs in a narrow cone around a sometimes upward-facing cut normal. The result looks like a round lamp and an orderly rising column. Camera framing now exposes the tear clearly enough to judge a different effect.

| Change | Preserved boundary |
| --- | --- |
| Authored pressure and sustained tongues use three unequal tapered hot lobes with an additional short birth lobe, ivory core and orange edge. Their local+X follows the actual world-normal projection into the camera plane. | Source cut position/normal, finite view-parallel fallback, camera, geometry and simulation unchanged. Generic surface kinds0–5 keep their existing shader branches. |
| Two authored smoke plates use an early dense belly and unequal charcoal billows, rising locally above the tear. | Same two slots, same original start offsets/lifetimes, owned sync/retirement and depth testing. No generic haze or new emitters. |
| Authored metal uses stratified outward sectors, cone .42–1.00rad instead of .18–.50rad, three dominant casing plates and ten smaller satellites at tracked severity2. Horizontal drag limits free drift and stronger gravity brings the plates down. | The existing18 fragment slots are13 metal/3 free sparks/2 retained embers in the tracked full burst. No fragment-count growth, new source geometry, physics impulse or event injection. Generic full bursts retain their original15 metal/3 spark behavior. |
| Rotated casing receives analytic support using the existing bent primitive bounds x±.5, y±.332, z±.85. | **Only the sampled birth-ground plane is known.** This is not a future terrain/slope collision query or full-course no-burial proof. Retained embers still follow the current moving cut and bypass the stale birth floor. |

Four admission files: one runtime FX file, two new test files and the existing `AuthoredAftermath` test. The one existing test contract change is **authored retained-hot surface kind2→7**; its count, opacity, root, lifetime and identity assertions remain intact. New kinds6/7/8 select pressure/tongues/smoke before the existing sand branch. The shared material program cache key changes; this does not add a draw or create a per-event material. Four burst plates, eighteen burst fragments, two separate existing contact plates/ten sparks, global16/64 ring capacities and eight draw objects remain unchanged.

**Validation:** final56 tests/8files pass; private TypeScript check passes. Tests cover projected direction under rolled cameras, exact roots, normalized and malformed normals, outward early ejection, three dominant silhouettes, more than8,000 transformed primitive-vertex floor checks, bounded free drift, selective identity cleanup, fixed-time idempotence, original expiry, ring saturation and resource identity. The source baseline comparison confirms identical visible arrays for all eight unowned legacy styles across six ages plus contact/shield reuse. Independent lifecycle/static review found no blocker. These are technical checks, not native force or art acceptance.

**First native gate:** the authored hot plane now stays at the exact cut/current sync root instead of the prior camera-facing `size*.25` displacement. Its asymmetric positive-direction tongues may extend into the opened gap, but that does **not** prove the hot root escapes casing depth occlusion. The native first-cut and early-impact images must show a clear rupture from that face. Depth testing remains enabled; no arbitrary offset or scenery hiding is included. Shader compilation itself also awaits the actual renderer. Density raises local overdraw inside the same fixed plate bounds; performance must be measured, not inferred from draw counts.

Acceptance still requires the unchanged six phase images and reference, actual dense native recovery review, and the established control/performance runs. No further camera work is included. If the root is hidden, fix placement from the actual source face/visible gap evidence before increasing opacity or global scale.
