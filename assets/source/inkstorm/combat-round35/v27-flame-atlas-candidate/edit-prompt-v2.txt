Edit the supplied four-frame flame atlas. Preserve its crisp hand-painted ivory/orange/rust flame style and the same four phases. Correct ONLY the canvas sizing, uniform grid, scale and root alignment.
Required output: EXACT 1024x1024 pixels, true RGBA transparent PNG. Four equal 512x512 quadrants, no borders or grid lines. The hot emission ROOT of each flame must land exactly at the center of its tile: image coordinates (256,256), (768,256), (256,768), (768,768). The root is the leftmost white-hot attached ignition point, NOT the center of the flame bounding box. All four extend rightward/+X. Preserve matched scale across the first three phases; last ember phase is intentionally smaller.
Fit all flames strictly inside their own tile with at least32 transparent pixels from every tile edge, including detached embers. In particular the third flame must not cross the vertical midpoint into tile4. Remove isolated alpha specks in empty space. Tight painted alpha edges, no translucent film across the background. Keep the four phases row-major: jagged pressure, curved pointed jet, broken hot pockets, dying embers. No text, scenery, particles outside tiles, checkerboard, shadows or vehicle parts. Do not output1254x1254 or a preview poster; the final deliverable itself must be1024x1024.
