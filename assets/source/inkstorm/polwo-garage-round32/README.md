# Polwo garage and lifecycle source repair — 8 September 2026

The failed browser run is preserved unchanged at `output/playwright/polwo-appearance-round32-final/receipt.json`. Its exact harness is copied to `polwo-appearance-acceptance.before-ordinal.mjs`; the copy hash equals the receipt script hash. `VehicleCardPreview.before.ts` preserves the preview source before this change. Hashes and CPU results are in `source-evidence.json`.

The lifecycle failure was an incorrect harness baseline, not an appearance identity regression: `beginRaceCountdown()` rebuilds the fixed event seed, and `rebuildRaceForCourse()` increments the in-memory ordinal. The recorded Start changed ordinal 0 to 1 with class, seed, course signature, event, profile, laps and stock upgrades identical. The repaired harness asserts exactly this one increment and unchanged mastery bytes, then uses the released identity as its baseline. It separately asserts the exact original garage identity after the later document reload. Appearance transitions still require full identity equality. The previous run passed through actual live Polwo and then failed this identity assertion; its 503/Retry stages were never reached. Its browser, owned process group and port were closed.

The garage screenshot `output/gauntlet/round-32-polwo/garage.png` shows a small dark craft within the wide host. Fitting already visits actual visible vertices. The bounded runtime change chooses camera direction (-1.5, .5, -.5) for active installed Polwo instead of the common (.7, .38, -1), then applies the existing inspection rotation and fit. This opens the long silhouette across the host and faces more toward the existing key light. Geometry, camera FOV, padding, material values, key light, image resolution, CSS and all other appearance directions are unchanged. Root owns the separate material and racing-camera corrections.

CPU tests exercise the actual private framing path with the packaged GLB positions/node transforms and a substituted renderer. On a 3.5:1 host, old projected horizontal coverage is 46.25%; candidate coverage is greater than 85%, with greater than 75% relative improvement. Every packaged vertex stays inside the existing horizontal/vertical padding and near/far clip planes at all twelve 30-degree inspection angles on square and wide hosts. The early 38% estimate used a different global bounds center and is superseded by this actual-path measurement. One initial test used that incorrect estimate; the corrected test asserts meaningful coverage and containment directly. Initial local tuple/Node-import/test-this TypeScript errors were fixed; final typecheck passed.

Validation: `npx --no-install vitest run tests/ui/polwoGarageFraming.test.ts tests/ui/VehicleCardPreview.test.ts tests/ui/vehiclePreviewReadiness.test.ts` — 12 tests, 3 files PASS. `npm run typecheck` PASS. Harness syntax and `--check` load PASS; no browser or build was run here. Candidate garage art acceptance remains pending actual capture.

The lifecycle harness also independently pins the actual public and served hero/rival GLBs. Normal-app Sola index 4 must admit Polwo's 28,830-triangle, 2-body/6-pilot rival package. Time Attack hides AI views, so the receipt records visibility rather than claiming visible rival rendering; Cup evidence is still required. Deliberate HTTP 503 routing remains exact to the hero URL only.


## Revision 1 Cup-scope correction

The next lifecycle run `output/playwright/polwo-appearance-round32-revision1/receipt.json` failed at its initial rival wait; it never reached the hero lifecycle. Browser, owned process group and port 64392 were closed. The exact failed script is preserved as `polwo-appearance-acceptance.before-cup-scope-revision1.mjs`, matching its receipt hash. The earlier statement about a hidden Time Attack rival is superseded: the event has only one actual player entry. Absent AI correctly stays procedural and does not request a rival GLB.

The revised harness has no rival waits during Time Attack. It completes and records every existing hero lifecycle/record guard first, verifies that Time Attack stayed a one-entry event without a Polwo rival browser request, and then uses the public Canyon Cup event button. The new stage explicitly asserts the intended event/profile/two-lap/+1 ordinal transition while retaining seed, course signature, class, stock upgrades and storage equality. Only then does it require eight actual entries, ai-sola at index 4, the admitted 28,830-triangle package and its real browser payload SHA. It captures the Cup admission separately; no completed Cup race or individual GPU timing is claimed. Syntax, no-launch load and whitespace checks pass. No browser or build was run for this correction.
