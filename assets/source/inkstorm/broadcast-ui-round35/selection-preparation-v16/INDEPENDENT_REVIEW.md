# Independent deferred-selection patch review

Reviewed the GameApp delta from GameApp.before.ts, including the follow-up room-exit and sector-label corrections. No remaining material defect found in the reviewed primary selection, Start, retry, Cup restart and room-exit paths. This is CPU source review; native motion/lifecycle acceptance remains pending.

The pending solo route stays separate from currentCourseSeed, preserving actual built geometry and guest authoritative seed rejection. Selection updates mastery/options without reconstruction while already awaiting. Reserve consumes the used-seed set immediately; same canonical event Enter preserves pending seed and option overrides. Explicit Cup restart forces preparation despite RaceMastery having already selected round one. Start always constructs with the selected full profile/loadout and then clears pending through rebuild. Exact-course retry still uses currentCourseSeed.

One gap was found and corrected: leaveOnlineRoom previously still rebuilt the catalog event.seed, giving Expedition its hero placeholder after pending was cleared on entry online. It now leaves/resets the existing race and reserves the proper selected solo route for construction at Start. Pending preview sectorLabels are now empty instead of borrowing the old course; actual launch still computes real labels. Initial pending=null is sound because the initial selected Trial already matches BASE_RACE_SEED=INKSTORM_HERO_SEED and its initial time-trial simulation.

Native acceptance should retain all deferred-selection proposal checks, especially true no-rebuild Arrow motion, exact Save/launch/retry identity, Expedition customization persistence on same-event Enter, same-seed different-profile launch, forced Cup reset/continuation, and two-client seed authority. The canonical-ID no-op is correct for native event button data; the private daily alias is not exercised by those controls. Existing courseSeedLifecycle tests do not by themselves prove GameApp's pending state.
