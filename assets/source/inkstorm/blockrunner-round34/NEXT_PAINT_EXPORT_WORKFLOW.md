# Blockrunner paint and export continuation

This is a copy-only continuation of the executed 44,028-triangle control fit, object signature `51ebd6d84ed2aa8a`. The complete source GLB, cleanup scene and fit scene remain the provenance masters. The original source pilot stays in the vehicle. The successful static source-fit review is 8/10; paint, export and gameplay are not yet accepted.

## Current paint boundary

The first paint/UV copy retained two meshes and all triangle surfaces, but subsequent corrected material renders exposed mirrored-transform winding that had not been reversed on flattening. Paint V1 and its failed renders remain diagnostic history. Its palette is not visually accepted. The replacement trial must preserve source polygon lineage and invert triangle/corner order only for negative-determinant source transforms. The originally requested inverse-transpose world corner normals remain the reference; a measured, declared 0.1-degree DCC encoding tolerance may be used on the derivative. Source and fit signatures and their corner normals remain exact. Feed the original requested normals again after UV work to avoid cumulative encoding error.

## Executable sequence

1. Root finishes a guarded replacement paint copy and compares driver, front and fullcraft renders against the actual fit cameras. Keep world transforms unnormalized for this comparison. Render copies must save and restore every polygon's material index when replacing copied slots. Two meshes do not imply two material draws.
2. Bake four private atlas channels using `mcp-safe/10-bake-atlas-channel-v1.py`, one owner/channel per call and always chaining the actual fresh state. Body receives 2048² base color and roughness; pilot receives 1024² base color and roughness. The existing procedural surface field is continuous in source world space, not assigned randomly per triangle. Do not bake lighting, specular reflections or an unreviewed normal map.
3. Finalize only the derivative materials: one body atlas material and four pilot response materials sharing the same pilot atlas pair. Preserve per-face suit/helmet/visor/glove roles from `pilot-paint-face-lineage-v1.json` and `mcp-safe/pilot-paint-reference-v1.py`. Retain UVs, triangle/corner lineage and the accepted normals. Replace procedural color/roughness with image inputs in the Principled material; roughness remains linear data in green. The target is five material draws before shadow/depth passes.
4. Render the finalized atlas copy at the same actual cameras and inspect each PNG individually. Compare color fields, wear scale, visor readability, hands, openings and silhouette with the accepted procedural trial. The approved B direction and concept 12 guide oxblood cowls, warm ivory body/discs, graphite frames and suit, ivory helmet and dark amber visor. The generated rear inset does not define engine geometry or anchors.
5. Export to a new private GLB, inspect its actual primitives, materials, images, UVs, normals and bounds, then reimport a disposable copy for matched-camera visual comparison. Packing or exporting successfully is not evidence of appearance parity. Retain file hashes, measured counts, exact export options and image lineage.
6. Apply normalization exactly once on a further export copy after paint baking. `normalization-candidate-v1.json` is an unapplied artist trial, not a canonical measurement: source −Y forward/+Z up becomes game +Z forward/+Y up; the proposed factor is 1.6 on the already imported source-world size. Verify final basis, pivot, pilot datum, bounds and actual exhaust centers/radii. Keep the physical crossbeam and omit artificial coupling anchors. Use the measured nozzle opening with the planned per-asset flame-radius contract; do not enlarge an opening to fit a default effect.
7. Package hero and rival variants separately. Current hero geometry is 44,028 triangles, below the existing 60,000 budget. For a 30,000-triangle rival target, retain the 4,128-triangle pilot and fit controls while reducing only suitable body detail on a copy, with explicit opening/silhouette checks. Do not use an unchecked whole-vehicle decimation. Body plus pilot color at 1024² and roughness at 512² is a useful initial hero texture trial (~13.3 MiB RGBA8 with full mips); rival color 512² and roughness 256² is ~3.3 MiB. Validate the actual packaged/decompressed footprint and appearance.
8. Only after source/export acceptance, coordinate the runtime appearance registration, shared model ownership/disposal, existing vehicle class, original-pilot handling, and CC BY attribution. Runtime CelMaterial can consume the baked color and roughness maps; start additional procedural wear/rim/reflection restrained to avoid repainting the baked result. Test the new hero and rival loading, selection, races, FX alignment, resource release and measured performance against the current frozen game. Prior vehicle performance evidence does not cover this new family.

No public asset, runtime code, gameplay class behavior or shared Blender save is authorized by this preparation alone. Root coordinates those later stages within the user's fleet request.
