# Imported exhaust mouths — round27

The first mechanical candidate adds a closed, stepped metal collar and recessed throat at each existing DCC exhaust anchor. Both mouths are one instanced mesh per racer, sharing one material and a384-triangle lathe geometry (768 submitted triangles across two instances per pass). They enter the ordinary MRT prepass, adding one beauty and one MRT submission when visible; the body-only shadow budget is unchanged. These are original runtime details around licensed preserved vehicle bodies, not changes to source model geometry.

The original hot-core cone, animation and anchor positions were retained. New metal geometry has its own explicit disposal, reused across appearance switches; two geometric/resource tests verify front-facing lip occlusion, an open central bore, authored transforms and one-time disposal. Existing appearance lifecycle tests remain passing.

First actual captures: `output/gauntlet/round27-nozzle-v1/` (Teemto,9images) and `round27-nozzle-v1-sebulba/` (Sebulba,11images). Both use build`index-DfPcQStz.js`, SHA`bc1798c416055347d19d4369fca6c6308aba92fa91dabcf8b96292c21cd2b323`, with zero browser errors. Sebulba is selected using the visible garage action and verified ready before capture; reload preserves the choice for the short live drive. Both owned browsers close in`finally`, with port5186 independently confirmed closed. These are actual world/vehicle captures, not generated images.

The frozen source map proves that these captures also contain launch terrain candidate2 (sourceSHA`a0ab709353a86372c02e238feaa76083f171f9be85c0a3a9c58a9b21d60ac357`) and the corrected contact pass. `source-attribution.json` records this explicitly; an unchanged-terrain A/B is not claimed. The forthcoming directional sky-fill and normal/roughness-map support are not in this build.

The fresh `BLIND_NOZZLE_LIPS_ROUND27.md` review retains the improvement at6.7/10 but fails the strict8 target. Real rim depth is visible on both vehicles; remaining problems are the broad bright cyan ring, pale lower throat facets, and abrupt casing-to-collar transition. The reviewer sees no definite floating gap or background visible through the mouth.

Second candidate, captured: restrict the imported jet radius to0.443m to fit the0.44m throat, darken the inner metal, reduce specular/reflection that made it look like a clean cup, and extend the outer return into the casing. Existing exhaust anchor/world-position tests and mouth/resource tests remain passing (16tests across two files). V1 sources are preserved under`assets/source/inkstorm/nozzle-lips-round27/v1/`. Neither candidate has a new full-race performance or strict art acceptance yet; short live intervals are not substituted for that gate.

Actual second-candidate frames are preserved in `output/gauntlet/round27-shadow-nozzle-v2/` and the later `round27-cliff-normal/` set. The latter uses bundle SHA `852a385778300327e717eef7eea3871265132fcad8abfa4bb01aa3b92e99cfe0` and adds the separately documented local cliff normal correction. Each set contains 12 actual images with zero browser errors. The fresh paired review in `BLIND_ART_ROUND27_CURRENT.md` retains B at 5.5 versus A 5.0, but rejects both against the 8 target: the mechanical interior reads more clearly, while the source still resembles a small dim fan rather than a deep powered chamber. Different critics' absolute scores are not a calibrated trend.
