# Round 35 UI/combat — frozen V32r1 release

[Release report](RELEASE_REPORT_2026-09-09.md) contains the implemented scope, exact build, verification and remaining work. The [publication and current QA archive](https://github.com/TheDudeCommits/PodRacing/releases/tag/inkstorm-v32r1-20260909) records the pushed commit and deployment. The [pre-release V31 handover](evidence/round-35/pre-release-v32r1-documentation/ROUND35_UI_COMBAT_HANDOVER.md) preserves the preceding state.

V32 admitted ability instrument styling, measured warning/feedback reservations and left/front/rear presentation-support dust. V32r1 adds actual local wreck phase to the per-render HUD handoff, closing the native pause/resume one-frame leak. Gameplay and simulation authority are unchanged.

Per-piece FX uses two porous cloud parents per part: 48 parents / 96 cards at capacity, 192 contact triangles, one contact mesh and nine effect owners. Ten grit entries per wreck are redistributed 3/3/4. Nine terrain probes per parent mean 54 changing-age queries per wreck / 432 at capacity; held FX age issues zero. The producer adds at most two center samples. The .18/.18/.10 m witness margins describe presentation supports, not new simulation collision. Source geometry and rig/camera trajectories remain unchanged by V32. These contracts do not establish visual weight; the fresh critic remains 5/10.

Four native cases plus the high-speed case pass. Corrected resume frame 916 remains suppressed, and all 647 actual wreck/recovery observations hide driving feedback. A fresh Heat Lance → Kodo kill at frame 6141 passes outgoing attribution, slowdown and camera checks. The 17-capture UI run and 3,923 native / 450 grouped / 18 protocol checks pass. Unchanged menu pixels mean critic score variation is not code progress. The 41 post-GO checks pass; 54 optional GO checks and a retained image do not override UNAVAILABLE after the group dissolves.

The primary crash cut at frame 227 / 9.08 s follows ordinary frame 226 / 9.04 s. Ordinary driving returns at frame 334 / 13.36 s after protected frame 333 / 13.32 s. Six full phases and 185 consecutive frames were reviewed. The 4.96-second crash clip uses source 8.76–13.72 s, exclusive; the 5.80-second UI clip uses 2.56–8.36 s; the 5.00-second outgoing clip uses 51.60–56.60 s. Clips have no added retiming or overlays; the source recording's 25 fps is not engine FPS. A high-speed supplementary locator identifies cut 260 / 10.40 s after frame 259 / 10.36 s; its fuller media review was stopped for release.

[Exact performance summary](evidence/round-35/full-race-v32r1-summary.json): Time Attack has 3,797 intervals / 63,780.8 ms; Cup has 8,150 / 137,827.7 ms, with no racing exclusions. Mean cadence is 59.532022 / 59.131800 Hz; p95 is 16.8 ms for both; maxima are 33.4 ms; intervals above 25 ms number 30 / 120, with zero above 50 ms. Adaptive DPR ranges are 1–2 / 1.875–2. Four file comparisons, six artifact boundaries and seven categories agree. Owned port 61171, process group 86542 and server 86588 are closed. The audit preserves its p95 template correction and original failed audit log; no native gate changed.

Current receipts, critics and original references are in the curated release archive with hashes. Historical output stays local; source, package and fixture evidence stays in the repository, with large assets in LFS. The source-only Ivory finish and resolved 260-face V2 torso plan are not a shipped fifth vehicle.
