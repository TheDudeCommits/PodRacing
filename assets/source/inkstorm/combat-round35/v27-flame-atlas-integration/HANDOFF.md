# V27 rooted flame atlas — private review candidate

The actual V26 critic remains5/10 FAIL. This trial replaces only authored rupture pressure/fire pixel shapes with the original painted four-phase atlas. Procedural smoke/contact effects, generic effects, vehicle geometry, simulation, camera and all plate transforms remain unchanged. No public copy or live source change has been made by this preparation.

Use `v27-flame-atlas.patch` only after its manifest input hashes match. Six files: one owned atlas helper, GalacticEffectsView, two bounded GameApp changes (boot URL and scalar snapshot), and three tests. Root will copy the unchanged source PNG to `public/assets/fx/inkstorm/rupture-flame-v1.png` and hash the served asset independently. Source: `../v27-flame-atlas-candidate/flame-atlas-raw-v1.png`, SHA256 `f14657a3388bf9618d0206806c7b9c54856bd7f54c98596ea1790d85c97dd4c5`.

The four nonuniform source rectangles and roots are explicit. Shared scale is `severity/240` metres per PNG pixel (1/120m at severity2). Per-instance source spans cancel the existing plate pulses and unequal X/Y scales; matrices themselves do not change. Each witness maps to its own existing plate center. Pressure and sustained fire retain their distinct V26 centers, including the existing0.6m fire offset. Local+X retains the projected real tear normal. Image Y is explicitly inverted from top-down pixel coordinates.

Pressure uses frame0 during its existing0.085s slot. Fire uses frame1, blends to frame2 at fire age0.24–0.40, then to frame3 at1.35–1.65, with the existing2.05s expiry/opacity. Loading does not restart any clock. Crossfades combine premultiplied color before returning straight alpha to the current transparent material. Texture is sRGB/linear, no atlas mipmaps, clamp-to-edge plus per-rectangle half-texel sampling and transparent-outside. Raw-v2's baked checkerboard is rejected and never referenced.

No request occurs for default CPU views. GameApp opts in once at boot. Until successful decode/dimension validation and uniform assignment, the exact procedural branch remains available. A view owns one texture handle; error/disposal/late completion tests preserve fallback and prevent resurrection/double release. Snapshot `game.flameAtlas` returns only `{status,url,width,height,frameCount}`; ready means a loaded texture is assigned to shader uniforms. Source PNG alpha/checksum is tested off-GPU; root's native guard verifies actual served bytes. No browser pixel readback or extra image work is added per frame.

Validation:51 focused tests across9files PASS; private TypeScript PASS. The new actual-PNG test decodes all pixels without saving an image, verifies the source checksum/RGBA format/isolated rectangles/ivory roots and checks every meaningful alpha pixel against the smallest existing plate extents and protected hot-radius envelope. Maximum radius at severity2 is3.96548m, inside the unchanged4.608m protected hot-volume radius; no terrain claim beyond that retained birth-envelope contract. Existing lifecycle/contact/depth tests pass. Mid-event load completion preserves every visible matrix/color/age/count, with shared pixel/world spans checked on both hot phases.

Initial focused run passed; initial private TSC failed on test texture generic typing, missing test-only Node type declarations and the private GameApp barrel remap. Those were corrected without changing production behavior or weakening assertions; both logs remain. No tests were timed out or geometry gates reduced.

Resources: unchanged8draw owners, existing4rupture plates/18burst fragments and bounded pools. Adds one texture and one vec2 instanced attribute (8bytes per existing plate/plume capacity), plus one existing shader program revision. One texture fetch normally, two only during phase blends. Default/failure paths still use the procedural shader. Native first-cut root visibility, sRGB appearance, phase popping, smoke balance, actual performance, and the unchanged fresh critic remain **pending**. The support-pivot rig is a separate private candidate; this patch does not alter it.
