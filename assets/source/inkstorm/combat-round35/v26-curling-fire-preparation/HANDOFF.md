# V26 private curling-fire candidate — not applied or rendered

The actual V25 [standard sequence](../../../../../output/playwright/round35-combat-v25/temporal-review/REVIEW.md) and [separate high-speed sequence](../../../../../output/playwright/round35-combat-high-speed-v25/temporal-review/REVIEW.md) show a visible hot root but persistent straight spoke shapes, plus a tall nearly uniform tan smoke column partly masking the torn machinery. The unchanged fresh critic remains **5/10 FAIL against 8**. Target17 is design guidance only; it was not sent to that critic.

The source cause is concrete: V25 unions closest-point distances to straight tapered segments; varying erosion cannot bend those segments. Both pressure and retained heat reuse that contour. Two heavily overlapping smoke silhouettes use one vertical color gradient, so their internal lobes do not read as distinct shaded masses.

`v26-curling-fire.patch` is a four-file private patch, **15,073 bytes**, SHA-256 **988ec27fbd5f8c996a82c8c3240837c87f5366c8b879d2281e4a9092f4b50104**. The source before-copy and all exact file hashes are retained in `before/` and `file-hashes.json`. No live source, public asset, camera, HUD, harness, simulation or dist change was made here.

The authored branch now uses:

- An irregular connected pressure mass lasting **0.085 simulation seconds**. Retained fire starts at **0.065 seconds**, creating a 0.020-second overlap instead of a dark gap. Its retained lifetime remains 2.05 seconds. At the current 0.18 slow pacing the pressure interval is about 0.47 wall seconds; that conversion is not a native timing measurement.
- A separate fire contour assembled from advected curved lobes, with a carved curl and broken orange/ivory hot pockets. Spatial curvature changes with event age; no tapered-ray helper remains. Local +X still follows the actual cut normal projected into the camera plane. The tight hot root is retained.
- Two smoke forms (surface kinds **8 and 9**) with different mirrored lobe arrangements, open density channels and a clear lower skirt. Each nearest lobe provides a curved light response and warm underside. This is authored billboard shading, not a scene light or volumetric simulation.
- Explicit event age in the existing authored surface attribute’s third channel. Moving-cut synchronization does not restart phase time. Legacy kinds and seed semantics remain unchanged.

The existing hot centers, projected normal basis, smoke center trajectories, plane sizes, depth testing, culling, casing/ember/contact trajectories, geometry and materials/resources remain in the same flow. There are still **4 rupture plates and 18 burst fragments per full rupture**, 16 global plate slots, 64 global debris slots and the same **8 draw-budget owners**. Two contact plates/sparks remain separately bounded as before. Shader silhouette pixels remain inside the existing planes. The inherited casing floor guard uses the birth ground plane, not new future-terrain queries; this patch makes no broader terrain-clearance claim.

Validation: `focused-final.log` is **58 tests / 9 files PASS**; `typecheck-final.log` is a successful private TypeScript check. Exact live-before byte checks and `git apply --check` pass. The suite retains the actual casing-vertex/birth-floor checks, malformed-normal fallback, all eight legacy-style visible-array parity, moving-root ownership, expiry, recovery, ring saturation and resource identities. New tests verify pressure-only birth, the overlap, pressure absence at 0.09 seconds, continued fire/smoke, monotonic age and no reignition from sync.

Two existing test contracts change explicitly: the all-four-plate orientation and peak-capacity observations move from **0.10 to 0.08 seconds**, because the shortened pressure correctly expires before 0.10; their matrices, peak counts and safety assertions remain. The new phase test verifies the later three-plate state instead of preserving an obsolete peak-duration assertion. Smoke slot IDs now distinguish 8 and 9. No source geometry or no-burial threshold was relaxed.

**Native gates still pending:** first-cut pressure visibility at the exact root with depth enabled; clearly distinct pressure/fire phases; irregular moving fire rather than a new smooth blob; two readable shaded smoke heads without hiding the tear; unchanged recovery and generic effects; and actual final native cadence/full-race performance. TypeScript/tests do not compile or perceptually validate the GPU shader. The authored fragment shader performs additional local noise/math, so runtime cost must be measured. No new camera tuning or expanded particles are proposed.
