# V18 crash candidate — source freeze, native judgment pending

The candidate changes the visible aftermath of the admitted Teemto damaged model. Its three major engine masses now blend toward different broad-side orientations along the sampled terrain grade, while the camera chooses the actual torn side and forward quarter. This is a renderer animation, not collision simulation. Original intact/damaged GLBs, source pilot, textures, physics, and reset behavior remain unchanged.

The existing four crash plates now include a smaller retained hot fracture and two directional smoke plates for roughly two simulation seconds. Two of the existing 18 debris slots become phased embers. Their position follows the displayed torn surface using racer index plus crash count; missing/recovered/changed identities retire only those retained effects. The fixed 16-plate/64-debris pools and eight existing effect objects remain. Root applied the narrow GameApp patch: attach that identity at emission, synchronize after the existing pose resolution, and update FX once after the racer loop.

The prior V17 source copies and hashes remain in `before/` and `before-inventory.json`. `source-freeze.json` pins current files and evidence. Parent owns the combined build, native capture, and any other GameApp changes.

Validation completed before freeze:

- The focused candidate suite passed 64 tests across six files, including actual hero/rival model positions and projected camera bounds. Maximum fragment terrain queries were 244/203; the prior bounds remain 296/222. Three major settled engine axes stay within the asserted terrain-grade constraint.
- Actual hero/rival minimum rear-fragment terrain gaps were 0.09120/0.10000 m. Settled left/front witness gaps were approximately 0.18 m. All tested visible source vertices remained above terrain. These fixtures do not establish every possible course pose or visual quality.
- Independent synchronization review found a stale birth-ground floor for moving embers and two coincident ember paths. Both were corrected before freeze, with regression coverage. The final corrected FX/contact suite passed 45 tests across four files; TypeScript and `git diff --check` passed.

Native review must judge whether the new side exposes the rupture, the masses read as resting instead of balancing, and the retained heat/smoke provides a coherent aftermath. No new native image or quality score is claimed. The brief restoration crop seen in V17 is intentionally unchanged for this first candidate; inspect the full return-to-chase sequence before deciding a bounded camera correction. Tests do not establish FPS, art acceptance, or final overhaul completion.
