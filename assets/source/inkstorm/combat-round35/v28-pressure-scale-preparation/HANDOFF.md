# V28 authored pressure and casing scale candidate

Frozen patch: `v28-pressure-scale.patch`, 13,823 bytes, SHA256 `73f6fdb6e0660c6f8ed588cd119dcd25a10d5078336bdcc509972f778ce584fa`. `manifest.json` pins all five before/candidate files and the preserved raw/public PNG. Parent centrally applied this exact candidate; this preparation did not mutate live source or public files. Integrated/native validation is pending.

The actual V27 review found crisp, attached fire but weak impact beside large machinery; the unchanged critic remains **5/10 FAIL8**. This candidate makes the existing pressure artwork 1.8× larger for its unchanged 0.085-second slot. Fire starts 1.6× larger, contracts smoothly to1.15× over its first0.60 seconds, and keeps the existing phase transitions and2.05-second duration. Quad basis and culling sphere grow together; UV span compensates so every source crop and root stays attached without cutting off the drawing. The two smoke layers, their matrices/opacity/timing, and the source pixels are unchanged.

Three leading metal fragments, indices1/7/13, previously all used angular sector1. They now occupy sectors0/2/4 of the same outward source-normal cone and are15% larger. Other fragments retain their paths, size and expiry. The existing analytical floor support accounts for the enlarged rotated primitive. This support uses the sampled birth floor; it is not a guarantee against later unsampled terrain.

Changed contracts are explicit: the old static1/120m-per-pixel at severity2 becomes an event-age optical envelope, shared by all concurrently blended fire drawings. The earlier generic4.608m birth-clearance assertion was not valid for authored source roots and was replaced by real alpha/quad/culling containment. An authored low root remains low; depth testing stays on, with no camera-front offset, artificial lift or terrain changes. Larger artwork can be naturally occluded. Pixel radius at severity2 is5.129m for pressure,4.991m for the initial jet, and5.023m for the first fully blended broken-fire phase; these are source-plane extents, not measured screen size or visibility.

Validation: **56 tests across11 files PASS**, private TSC PASS, and pre-admission `git apply --check` PASS. Actual immutable RGBA checks retain every meaningful alpha pixel within exactly one crop, preserve opaque root witnesses and transparent borders, and prove quad containment. New tests check pressure/fire hierarchy and unchanged expiry, low/moving source-root attachment, and three separated large-fragment lanes. Existing tests check every rotated casing vertex against the sampled floor, saturation/reset/ownership and texture late-completion/error/disposal. A private before/candidate comparison proves identical generic visible instance arrays for8 styles×3 normal states×7 ages and unchanged authored smoke, roots, ages, opacity, orientation and crop spans. Initial patch-format failure is retained in `apply-check-format-v0.log`; no runtime/test failure occurred.

Budgets remain4 rupture plates,18 birth fragments,8 draw owners, one already admitted boot atlas, no new terrain queries or scene objects. Native gates remain: first-cut root visibility and enlarged phase progression, natural ground/model occlusion, no rectangle or neighboring-frame bleed, readable casing punctuation, clean recovery and actual frame cadence. No visual pass is claimed.
