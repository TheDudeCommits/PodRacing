Initial integrated run:853/854passed; the hero full-source footprint test reached its default5000ms timeout at6017ms while still completing all geometry checks. Its original test and verify-v22.log are retained. The correction keeps every source-vertex iteration and the exact gap>=-1e-6 predicate (including NaN failure), constructing the detailed Vitest matcher only when that predicate fails. No timeout, sample, physical bound or broad-support gate is relaxed. Full verification is rerun in verify-v22-final.log.
