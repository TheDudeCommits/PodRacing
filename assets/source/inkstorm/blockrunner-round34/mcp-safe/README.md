# Blockrunner MCP-safe fragments — prepared, not executed

Use these replacements instead of the original fragments in the parent directory. The four original `.py` files remain byte-for-byte preserved. Root reported that the Blender MCP policy disallows `hashlib`, lambdas, filesystem/general-execution access, namespace introspection and dunder access; those operations are absent from these replacement fragments.

The replacements import only `bpy`, `collections`, `json`, `math` and `Vector` from `mathutils`. Named key functions replace lambdas. Required render variables are referenced directly before any scene mutation; missing inputs stop the script. No namespace inspection, source import, source edit, shared blend save, filesystem read or arbitrary code execution is included. Rendering still writes only the explicitly targeted source PNG through Blender's render operator.

Source structure signatures now use **FNV-1a-64 over canonical ASCII JSON**, encoded as a sixteen-character hex string. JSON keys are sorted with separators `(',', ':')`, `ensure_ascii=True` and `allow_nan=False`; offset is `14695981039346656037`, prime `1099511628211`, mask `18446744073709551615`. The externally computed expected hierarchy signature is **`25b9eb9de1488bd3`**, from the preserved source GLB's sorted `(name, parent name, MESH/EMPTY type)` rows. These are non-cryptographic structural comparisons, not SHA digests or proof of exact byte preservation. Source GLB/receipt SHA strings remain clearly labeled historical external provenance; no cryptographic module runs in Blender.

## Read-only audit payload

Concatenate these absolute paths in order outside Blender, then pass the resulting text directly to the official Blender MCP code tool:

1. `/Users/amir/Projects/PodRacing/assets/source/inkstorm/blockrunner-round34/mcp-safe/00-source-reference.py`
2. `/Users/amir/Projects/PodRacing/assets/source/inkstorm/blockrunner-round34/mcp-safe/01-source-guard.py`
3. `/Users/amir/Projects/PodRacing/assets/source/inkstorm/blockrunner-round34/mcp-safe/02-audit-existing-source.py`

The exact existing source scene and all source geometry/reference guards are unchanged. Capture `BLOCKRUNNER_RECEIPT_BEGIN` through `BLOCKRUNNER_RECEIPT_END` externally. Require measured status, matching FNV source signatures, unchanged preexisting scene/collection memberships and restored context. Report signature scope accurately. Do not reuse a receipt generated by the older SHA-based scripts.

## One-view render payload

Prefix the first two safe files, inject the actual successful **safe audit** receipt as `BLOCKRUNNER_AUDIT`, the desired `BLOCKRUNNER_VIEW` (`fullcraft`, `front`, `rear` or `driver`) and a fresh `BLOCKRUNNER_RENDER_TOKEN`, then append:

`/Users/amir/Projects/PodRacing/assets/source/inkstorm/blockrunner-round34/mcp-safe/03-render-neutral-view.py`

As before, root checks externally that `assets/source/inkstorm/blockrunner-round34/source-<view>-<token>.png` does not exist. One render per call; inspect each result before the next. Neutral original materials, copied scene/mesh/material data, source-derived camera framing and full cleanup are unchanged. Source anatomy/anchors are not accepted by the scripts. The generated concept's rear inset remains excluded from geometric decisions.

Static validation completed during preparation: all four safe files and both concatenated payload shapes parse; an AST scan found no disallowed import, lambda, forbidden builtin/name, dunder attribute or dunder definition from root's reported policy list. This is static review only. No Blender execution or claim of successful live policy admission has been made.
