The first V17 run passed16captures and all60catalogue equips/restores, then its stronger visibility check sampled the saved calendar in the opacity-zero first frame of its intentional entry animation. The failed receipt retains hiddenBy=viewport with otherwise clear geometry. Wait for that animation to finish before reading its text. Reuse the same full glyph visibility/viewport/clipping guard after equip to cover larger installed artwork too. No runtime change, removed gate, race staging or retrospective PASS.
