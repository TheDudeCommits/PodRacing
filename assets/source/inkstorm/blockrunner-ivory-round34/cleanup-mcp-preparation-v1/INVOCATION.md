# Ivory cleanup — prepared invocation order

**No Blender call has been executed by this preparer.** Both prepared stages fit the 200,000-byte UTF8 limit. [Static validation](prepared-validation.json) records hashes, sizes and the import/attribute checks. Imports in MCP code are limited to `bpy`, `json`, `math` and `Vector` from `mathutils`; there is no file IO, computed attribute access, module namespace passed as a value, render, import, export or blend save.

1. Run [01-ivory-disposable-normal-preflight-prepared-v1.py](01-ivory-disposable-normal-preflight-prepared-v1.py) serially while Blender is in Object mode and no render job is active. It is 96,855 bytes; SHA256 `8e95268cb9faba98f4e5859e61e7c3334929efd63cc75a0b9da8c004f9f0ad66`. It rechecks the original/source-copy signatures and exact live pairs, reconstructs one disposable newly copied mesh, measures every retained normal, removes the scratch mesh, and restores/verifies the fresh global context and ID sets. Preserve the JSON between `IVORY_RETAINED_NORMAL_PREFLIGHT_RECEIPT_BEGIN` and `IVORY_RETAINED_NORMAL_PREFLIGHT_RECEIPT_END` as the actual receipt.
2. Inspect `preflightPassed`, all preservation fields, the complete retained-normal result and its source-only angular/chord differences. A successful disposable execution is a measurement, not automatic art acceptance. The code imports no Colour normal tolerance. If this preflight fails, do not bind or execute cleanup.
3. Once the actual Ivory reconstruction is reviewed, run the external filesystem preparer with that receipt: `python3 compose-prepared.py /absolute/path/to/actual-preflight-receipt.json`. It reads only saved files and never executes Blender. It emits `02-ivory-cleanup-bound-prepared-v1.py` and `bound-validation.json`, including the new payload hash/size and the exact receipt SHA. Inspect those before invoking the bound MCP payload. The current [unbound template](02-ivory-cleanup-unbound-template-v1.py) is 115,616 bytes and deliberately refuses to mutate Blender without the actual proof.
4. Run the bound cleanup serially while no render job is active. It makes another 55-object/51-mesh/51-material copy, confirms unchanged parity before cleanup, rechecks own exact live pairs, and removes only the 4,828 upper-index opposing faces in the one target mesh. It verifies the reconstruction exactly reproduces the own preflight result, including normal signatures and differences. The other 50 meshes, all object transforms/parents, both beam placements, material graphs, and both preserved source scenes remain guarded. A failure rolls back only newly owned derivative IDs.
5. Retain the actual JSON between `IVORY_CLEANUP_RECEIPT_BEGIN` and `IVORY_CLEANUP_RECEIPT_END`. Require `cleanupPassed` and all source/source-copy/global preservation guards before any later authoring. The result should have 43,556 triangles, with 4,128 pilot and 700 body triangles in the cleaned container. Semantic source/retained indices are recorded as lineage only; no masks, pilot extraction or fitting occurs.

The exact pair indices are compacted as `[2i, 2i+1]` for 4,828 own Ivory pairs and independently reverified in live coordinates with reversed winding, identical matched-corner UVs and equal material slots. The five disjoint semantic subsets use 51 verified consecutive-index ranges, including the body remainder. This compression does not change their membership or classify the whole container as pilot.

Scene counts are sampled fresh, not hard-coded to the earlier 156 scenes. Exact original scene/layer/active object/selection is restored. The original GLB SHA, executed source-copy receipt SHA and full offline cleanup-input SHA are embedded in both calls.
