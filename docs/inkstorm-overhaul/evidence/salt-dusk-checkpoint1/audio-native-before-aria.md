# Final integrated audio lifecycle check

Tested the actual built game (`dist/assets/index-CCnFLqZi.js`) through normal UI with native Chrome Web Audio. No runtime source edits and no generated audio. Read-only node instrumentation records real source starts/stops, gain values and graph connections; receipt.json pins the bundle, owner, GameApp and intro bytes.

**11 checks PASS; 0 product checks FAIL; 2 visibility/pause checks unverified or not exercised.** All 27 catalogue files loaded with zero failed/pending files. Eight recorded vehicle loops are audible by nonzero native gain during the short race and exactly zero, including engine bus and canyon return, after returning to garage. Combat effects clear. The original intro finishes once at rate 1, and the same existing music excerpt source survives scene changes. Mute/unmute keyboard control persists across tab focus; the 40% music user setting applies to both selection and race mixes. The return-to-garage music sample is partway through its 650ms fade, not a settled-gain assertion. The in-game credits link opens creator/license attribution. No runtime oscillator or sample-buffer generation occurs.

Actual popup focus and a separate new-tab focus both left `document.hidden=false` and emitted no visibility event in this automated Chrome session. Therefore no native hidden-handler or hidden-plus-paused claim is made; those behaviors retain the 37 focused API test evidence. The garage settings surface is interactive but its dialog retains `aria-hidden=true`, excluding its controls from role-based lookup. Root was notified. No listening-quality, earned-overtake, full-race or FPS acceptance is implied.

The initial harness attempts are preserved: attempt1.json (More disclosure not opened) and result2-cli.txt (role locator excluded by aria-hidden). result3-cli.txt is the unchanged raw successful lifecycle observation; receipt.json explicitly classifies its unexercised visibility assertions rather than treating them as passes. mute-cli.txt adds actual keyboard mute and focus-return observations. Browser session salt-dusk-audio-final/PID43670 and server port49379 were closed immediately after the last browser check.
