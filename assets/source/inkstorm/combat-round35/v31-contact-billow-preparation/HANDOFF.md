# V31 private porous rear-contact billows — frozen native trial

The rejected V30 opaque compound caps read as small outlined mounds in actual native footage. This candidate replaces only that contact renderer with ten softly eroded, yaw-facing cloud cards rooted in the same five copied rear-contact parents. It does not change any measured contact, source birth band, engine trajectory, camera, rupture heat, simulation, network, or GameApp call.

Root viewed the private .4083s and 1.2083s component images and accepted their softer, airy appearance for a bounded native trial. This is not native visual acceptance or an FPS result. V30's rejected native evidence and all three opaque component attempts remain untouched.

## Exact scope

Three source files: GalacticEffectsView.ts, SolidEffectGeometry.ts, SolidEffectMaterial.ts. One existing test: tests/render/WreckContactVolume.test.ts. Exact original files are preserved under before/. FROZEN-MANIFEST.json records current live base and candidate byte hashes; v31-porous-contact-billows.patch is the only admission patch. No source changes occurred after the accepted first component preview; only final tests and output evidence were added.

Five parents still use the copied rear casing-edge anchors and their unchanged along-band positions, drift and owner/sequence lifecycle. Their cosmetic growth does not create new front/left contact witnesses or expand the reported measured footprint. Each parent carries two bottom-anchored vertical cards, separated by at most .24m along the copied outward direction. The carriers face horizontal camera yaw; their lower edges follow the cached terrain grade. The shader adds three unequal smooth lobes, porous extinction and a soft disappearing perimeter, with a world-sun crown/trough response. It uses ordinary transparent blending, depthTest=true, depthWrite=false, FrontSide, and no opaque ink prepass.

## Bounds and lifecycle

- Existing one cloud mesh owner; total owned effect children remain 9. Forty parent slots produce at most 80 cards. Two triangles per card: 20 triangles for one contact, 160 at full capacity (formerly 1,280 /10,240 opaque-cap triangles).
- State attribute is 1,280 bytes. Preallocated card matrix/state/depth/order scratch is 13,520 bytes. Stable insertion sort is bounded by 80 cards, normally 10; no new per-frame arrays/comparator allocations.
- At each changing age, nine actual terrain probes per parent: 45 per contact, 360 at capacity. The source-axis square radius is half of maximum current card width plus .24m separation, so it encloses both carriers at every camera yaw. Same age, including camera yaw changes, performs zero terrain queries.
- A conservative plane rises above the nine measured heights by .04m. This does not prove unmeasured terrain between probes or arbitrary discontinuities. Actual fixture card corners and nine samples along every base passed separately. Terrain can still occlude cosmetic dust naturally.
- Cards swell by .20s, remain rounded while eroding after .85s, and retire by 1.65s of game time. The near-expiry rounded-zero guard releases callbacks before any degenerate dimension/query. No wall-clock timer is added. Birth/reset/owner replacement/recovery clear the same pool; geometry and material dispose once. Existing generic point-emitter contact plates remain unchanged.
- Per-card far-to-near centre sorting improves instance overlap. Intersecting transparent cards and overlap with other transparent effect meshes remain an approximation, not exact volumetric integration. Higher pixel overdraw and procedural shader cost require the integrated cadence/full-race measurement despite fewer triangles.

## Focused evidence

Final compatibility: 72 tests in 13 files PASS (6.07s). Private strict TypeScript PASS. git apply --check --whitespace=error-all PASS. No live source/test/build writes.

Actual hero/rival source fixtures replay the recorded V30 brake/boost inputs to first rear contact at frame 920. The original V30 test replay and its closed-cap geometry expectations are preserved under before/; the new test explicitly changes the representation contract from five closed 256-triangle caps to ten two-triangle carriers, transparent/fading aftermath and 80-card capacity. It retains terrain clearance, deterministic held-clock behavior, owner wrap/reset/recovery/disposal, malformed terrain rejection and near-expiry guards. The 12 existing unrelated rupture/contact/atlas compatibility files are unchanged.

At the observed V30 eye, using a declared chosen component look target, the full carrier span along camera-right is 7.45935m hero /7.58406m rival, with peak vertical rise 2.59013m and summed carrier rectangle area 66.80873 square metres. These are carrier bounds, not opaque cloud area or exact native pixels. Actual camera quaternion was not recorded. Across four yaw quadrants plus that eye, minimum sampled base clearance is .037206m hero /.035072m rival. First yaw uses 45 terrain probes, all later held-clock yaw changes use zero. The full 80-card capacity sort and directly-over-parent finite orientation fallback pass. Exact rows are in final-metrics.json.

Private WebGL phases .05833, .14, .40833, 1.20833 are preserved in output/playwright/round35-combat-v31-component. The early .05833 phase starts as small puffs; the later compound billows soften and overlap without the opaque mound outline. Renderer shader/GL/console errors and warnings are zero; actual WEBGL_lose_context restore passed. The component has actual rear contact/terrain and observed eye, a chosen target, and no vehicle, so it cannot establish casing visibility or native composition.

The separate component depth check uses a neutral opaque foreground box and a test-only three-metre downward carrier translation, restoring all matrices afterward. It measures 17,737 nonzero visible cloud pixels; the foreground box region has zero cloud leakage. Actual terrain hides all 17,467 otherwise visible buried cloud pixels. Shader/GL/console errors are zero. This is isolated rendering validation, not a gameplay impact, geometry burial change or an actual casing test. RESULT.json and exact browser commands are preserved. Both owned browser sessions and owned port5174 servers are closed; unrelated Orbhold port5173 was left untouched.

## Native admission limits

No score target is claimed. Root must judge actual low-speed contact visibility and airy silhouette in the full scene, retaining the unchanged six phases plus original-reference blind critic. Inspect the separate actual high-speed sequence as a clearly labelled supplement, not a replacement for the primary comparison. Native camera/terrain/casing occlusion, alpha overlaps and the measured integrated performance remain pending. All private GPU/CPU test jobs are complete; source is held for central admission.
