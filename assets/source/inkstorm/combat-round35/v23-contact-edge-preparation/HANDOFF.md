# Private V23 contact edge handoff

Ready for central review/application. No live source, tests, scripts, public assets or dist writes were made. Browser/GPU/Blender/build were not used. All CPU commands are finished.

V22 geometry hypothesis: at reproduced frame 932, lateral rays from 10/15 hero and 11/15 rival center-fan sample points intersect the complete casing on both sides. Its contact-band width is 1.127752 / 1.275493 m, while the complete source casing projects to 4.868069 / 4.712839 m. This source-geometry result supports center-fan occlusion; it does not measure native pixel visibility. Detailed raw rows are in baseline-hero.json and baseline-rival.json. The parent separately owns V22 actual video interpretation.

The five-file patch changes three runtime sources plus two focused tests. WreckGroundFootprint gains an optional readonly pair of opposing edge vectors. TeemtoAuthoredDamage derives these from actual cached source extrema and their existing terrain samples. Each retains its measured source XZ; subtracting its corrected gap from the corrected world Y recovers its own terrain height. These are terrain projections beneath full casing extrema, used as cosmetic sand escape roots. The original minimum-clearance witness remains the sole measured contact. Elevated outer source vertices are not claimed to collide with ground.

GalacticEffectsView copies a valid pair into its existing TWO plate slots and directs their unchanged 3.2 / 4.8 m/s departures toward the two exterior sides. It retains all existing dimensions, opacity, shader/program, depth testing, duration, particle count and pools; the ten sparks are unchanged. Optional edges must be finite, on opposing sides outside the near-ground band, within 6 m laterally/vertically, and within the half-length plus 4 m longitudinally. Invalid edge data uses the admitted V22 center-footprint emitter; omitted/invalid whole footprints retain the original point emitter.

The geometry update now transforms every existing cached support point when finding lateral extrema, instead of only near-band points. This is bounded extra matrix arithmetic, not a new source-vertex scan or terrain query. The grounding pass, pose matrix, minimum witness, dedup gate, GameApp adapter, birth/reset and simulation remain unchanged. There are no new slot fields, scene objects, materials, GPU programs or timers.

Validation: 33 focused tests across 4 files passed (1.96 s); 9 compatibility tests across 3 files passed (1.49 s); private TypeScript check including both changed tests passed. Separately, the initial two actual-geometry diagnostic cases passed (1.64 s). The 42 focused/compatibility tests include complete source-edge coordinate matching for hero/rival, exact ground heights, no runtime source reads, existing query ceilings, outward complete-triangle rays, unchanged witness/repeat/reset/pilot/simulation, two correct FX heights and outward signs, mutable-input isolation, invalid-edge fallback, complete ring wrap, existing contact dedup/FX regressions, and exact buffer parity against the pinned V22 emitter for point and legacy center-footprint inputs.

Commands and logs are preserved locally: focused-initial.log, compatibility-final.log, typecheck-initial.log, typecheck-with-tests.log, baseline-diagnostic.log. The before-runtime effect module exists only for the independent V22 parity test. test-runtime WreckVisualPose and TeemtoWreckBreakup are unchanged support copies so private TypeScript resolution follows the new optional contract; they are not patch files.

Native visibility, slope appearance across the full fan span, performance and visual acceptance remain unverified. Root already reviewed the three source diffs without blockers; apply and native execution remain centrally owned. candidate-inputs.json pins exactly the five intended patch inputs/outputs.
