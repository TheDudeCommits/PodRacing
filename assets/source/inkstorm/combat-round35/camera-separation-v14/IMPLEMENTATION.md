# V14 camera and breakup candidate — executed source changes, visual review pending

V13 remains a rejected visual checkpoint: actual breakup activation passed, while the fresh seven-image critic scored 4/10. See the preserved [V13 temporal review](../../../../../output/playwright/round35-combat-v13/temporal-review/REVIEW.md) and [critic](../blind-camera-v13/cli-critic-attempt1/review.md). Concept15 is target guidance only; no geometry was taken from it.

This candidate changes the Teemto engine opening from .26 to .12 simulation seconds. The original angle/translation envelope, exact intact birth/reset, per-engine terrain support, source geometry/materials, cockpit/pilot and simulation state remain unchanged. The existing geometry preparation now provides stable world-space camera arrays: eight rigid source-box corners for fallback families, or24 corners from the stationary cockpit/pilot and two independently transformed engine boxes. No per-frame vertex scan, geometry clone, material upload or new draw is introduced.

The combat camera uses these corners to solve perspective containment instead of inflating a21m assembly sphere. Its direction is approximately60 degrees off the rear axis and11 degrees above horizontal. The fit reserves X±.78 and Y[-.58,.70] in normalized screen coordinates, including caption/matte room. It checks the spring’s intermediate angle as well as the desired endpoint. At most two terrain height queries per fit keep the eye above nearby ground; this is not scenery occlusion detection. Invalid or missing corner data retains the former finite sphere fallback.

Only an actual victim cut opts into the protected camera path. The director still restores its chase/manual mode; manual/cockpit/replay views, reduced-motion cancellation, pause, and outgoing attacker policy retain their existing boundaries. After the matte, the fitted three-quarter view continues through wreck and recovery, then the ordinary chase spring resumes at running. Recovery uses intact bounds, including fractional-tick translation without restarting tumble. This aims to avoid V13’s immediate rearward transit through the gantry without hiding scenery. Driving input during recovery remains simulation-owned and may already be accepted; the protected view does not defer control authority.

Validation is CPU-only:

- Existing camera/pose/import group:41 tests PASS before the added opening test; updated breakup file:8 tests PASS.
- New actual-model framing suite:5 tests PASS. It projects86,923,968 actual source POSITION samples, all four admitted families, all four tested aspect ratios, and cached corners over1,088 sequential updates. This is geometric substitution on the existing native Teemto terrain/input trajectory, not four independent driving runs or FPS evidence.
- Typecheck and scoped diff check PASS. The initial typecheck failure (wrong comfort field and a fixture missing the new bounds field) is preserved; both were corrected before successful final validation.
- [Projection measurements](wreck-bounds-framing-projections.json) and [coverage](wreck-bounds-framing-coverage.json) retain all results. Landscape held-shot fixtures922/934/943 reach65–80% source width. Exact intact birth is smaller; portrait held poses and some later hypothetical side views remain below65%. Containment passing does not mean every composition target passed.

Remaining risks require actual V14 capture: close/low scenery occlusion, perceived rupture scale, the large intact-shell appearance, portrait composition, protected recovery orientation while control may be available, and the eventual return to ordinary chase. Source corners conservatively include empty volume. No terrain/course/physics/network/public asset or FX/UI changes were made by this camera task. The final [source inventory](source-freeze-inventory.json), immutable `before`/`frozen` copies, and [candidate patch](v14-camera-separation.patch) preserve the exact scope.
