# Blockrunner normalized package and runtime admission preparation

All work here is private/source-only. Root executed the V3 and final V5 exports; this worker performed CPU packaging, validation and isolated tests. No live runtime/public files, Blender session or browser were changed by this worker. The frozen foundation performance checkpoint is historical; root separately applied the exhaust API after that freeze.

## Actual final V5 package result

The [V5 pipeline](packaged-atlas-v5-v1/pipeline-steps.json) passes all six stages. Hero is **3,692,972 bytes / 44,028 triangles**, SHA256 `576c198464696004a46c8adc14f48d54004ea16bd1cb86b15ddeaad102a7c908`; rival is **3,228,580 bytes / 29,682 triangles**, SHA256 `a411d5bee15f6b1d10fab253002aaef6f014e69055ce24bc001011c8d88b92dc`. Both have five draws, four owner-specific atlas images, identity roots, no COLOR attributes, and Khronos zero errors/warnings/infos. The final geometry and all NORMAL/UV arrays match the calibrated V3 geometry exactly, so the rival preserves its 8,952 protected engine faces and complete original pilot, with the same measured limitations below. V5-v1 files are immutable; any sampler/material revision must use a fresh package directory and fixture.

The staged seven-file registration patch and checked-in fixture now pin these actual V5 packages. `git apply --check` passed; [V5 isolated checks](focused-checks-v5-sola.json) pass **42 tests / 5 files** with no environment dependency. The preceding complete candidate is preserved in `runtime-candidate-BEFORE-V5-FINAL/`. Actual in-game rendering, hero/rival appearance comparison and final performance remain root-owned downstream gates.

## Preserved V3 pipeline result

| Candidate | Bytes | Triangles | Draws | SHA256 |
| --- | ---: | ---: | ---: | --- |
| [Hero](packaged-atlas-v3-v1/blockrunner-hero-v1.glb) | 3,748,076 | 44,028 | 5 | `6451e78bc4933fd294bc0c080dbedcf679b2d558b118b62cca8ce4812f942b02` |
| [Refined rival](packaged-atlas-v3-v1/blockrunner-rival-v1.glb) | 3,443,832 | 29,682 | 5 | `d3a1489720f4b46cf2b8f41cb63f1794e587a4b88a9139e20cfafc5da3465d7c` |

Hero has 39,900 body + 4,128 original pilot triangles; rival 25,554 + 4,128. Both have one body and four semantic pilot primitives, identity mesh roots, four atlas images, no COLOR attributes or normal maps, and no additional driver. Hero images are 1024 base / 512 roughness per owner; rival 512 / 256. Base filtering uses linear-light sRGB; roughness remains linear data in G. Hero/rival Khronos reports contain zero errors/warnings; hero has zero infos, rival one unused old-index-accessor info. Each package receipt pins actual bytes and texture sizes.

The complete final-atlas orchestrator was also exercised on the same actual V3 export in [packaged-atlas-v3-reuse-pipeline-check](packaged-atlas-v3-reuse-pipeline-check/pipeline-steps.json): all six package/reuse/validator/fixture steps passed. It removes the unused old rival index accessor, so both validation reports have zero errors, warnings and infos. The original V3 packages above remain unchanged; the final atlas has not yet passed this sequence.

Positive uniform exported transforms are baked into POSITION only. Maximum hero float32 POSITION write error is **0.468319 µm** relative to the actual exported transform. Encoded Blender transform versus the nominal double-precision normalization candidate differs by **1.799556 µm**, reported separately. NORMAL/UV/index accessor bytes remain unchanged at packaging; final mesh roots carry no scale/rotation/translation. Pilot datum stays nominally Z=-5.2, body scale is 1.6, and +Z/+Y are forward/up.

Rival locks preserve **all 8 engine/intake/throat components, 8,952 triangles**, and the entire pilot. Body attribute arrays are exact; only body indices change. A source comparison applies the actual mirrored corner lineage before checking the protected ranges. Earlier observers/LOD failures are preserved. The one measured-deviation refinement adds 1,381 locks, 13,193 total. **The 2 cm trial criterion still FAILS:** 244,090 bidirectional samples peak at **0.024842488 m hero→rival / 0.021002364 m reverse**, with 35 samples above 2 cm. These are seven finite barycentric samples per changed triangle with exact nearest-triangle queries, not a continuous Hausdorff bound. The unchanged 30k cap passes; actual silhouette/texture comparison decides whether to retain this private candidate. [Measured samples](packaged-atlas-v3-v1/rival-v5-independent-surface-samples.json).

## Reuse with the actual final atlas

1. Run `compose-export-payload.py --state ACTUAL_FINAL_ATLAS_RECEIPT.json --token A_FRESH_SAFE_TOKEN --output NEW_PREPARED_INPUT.py` with paths inside this source directory. It consumes only used current-state fields, includes exact attribute-signature helpers, rejects stale/incomplete state and enforces a UTF-8 payload below 200,000 bytes. Root executes that payload through the guarded existing Blender MCP workflow; the composer never calls Blender.
2. The copy/export fragment keeps source/fit/paint scenes and context, copies exactly two owners, applies scale/translation once, and never saves the shared blend. On new copies only, it verifies/removes `Inkstorm V4 Edge Distance` and `Inkstorm V4 Edge Strength` FLOAT_COLOR/CORNER masks, records removed value hashes, and checks all non-mask geometry/UV/material indices and corner normals unchanged. Original mask values are checked against the supplied actual receipt before/after. No other color layer is silently removed; the CPU packager rejects any COLOR_n.
3. Use the bundled Python with `run-private-package.py --master ACTUAL_NEW_MASTER.glb --expected-master-sha256 ACTUAL_SHA256 --export-receipt ACTUAL_EXPORT_RECEIPT.json --output-directory NEW_PRIVATE_DIRECTORY`. It packages hero, reuses calibrated rival topology only if every current hero POSITION/NORMAL/UV/index array and transform exactly matches the calibrated V3 hero, packages rival, runs Khronos, and generates a fresh `blockrunnerPackage.json`. It preserves failures and refuses overwrite. If geometry arrays differ, stop and recalibrate instead of guessing a correspondence.
4. Compare final hero/rival actual renders. Preserve the V3 fixture/candidate before replacing it with the generated final fixture and package bytes. Apply the staged registration only with the final concrete assets and root coordination.

Bundled Python: `/Users/amir/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3`. Node tools use the already installed `/tmp/inkstorm-rock-lod-tools`; no download is performed. The package resampler reuses the read-only existing Polwo helper's color/data filtering, without invoking its model-specific packaging function.

## Registration scope and checks

[Candidate patch](runtime-candidate/candidate.patch) adds Blockrunner metadata, canonical materials/attachments, selector enumeration, 20001748 / Pod Racer Colour / CC BY4.0 credit, checked-in byte fixture and focused tests. It uses root's existing `exhaustApertureRadius: .34` API and authored hardware, omits duplicate coupling, and leaves generic 60k/30k/12-draw budgets intact. Its asset tests enforce the stricter 5/1/4 draws and owner image sizes. Unknown IDs still fail closed. Course, physics, records and vehicle class are unchanged.

Root requested **ai-sola**'s cosmetic preference become Blockrunner; ai-rax remains unchanged. The actual ordinary grid contains only one podracer AI, Sola. Tests assert unchanged race classes/snapshots and its real rival URL; no harness should force an AI class. Final browser runs must wait for actual ai-sola Blockrunner readiness in Cup, never require an absent rival in Time Attack.

Both V3 and final V5 isolated source/asset stages pass **42 tests / 5 files**, including real package headers and production roster routing with synthetic render fixtures. [Preserved V3 receipt](focused-checks-v3-sola.json), [current V5 receipt](focused-checks-v5-sola.json). Earlier missing-fixture collection failure and successful intermediate runs remain. Ordinary tests read `tests/fixtures/blockrunnerPackage.json`; **no external environment is required**. These checks do not establish actual browser rendering, performance or art acceptance. Root owns final build/capture; performance wrappers remain blocked on the final bundle/SHA and explicit browser-free signal.
