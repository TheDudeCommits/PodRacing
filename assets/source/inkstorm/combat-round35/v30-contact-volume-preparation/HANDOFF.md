# V30 closed rear-contact dust — frozen private native trial

The V29 two-plate contact effect is replaced only for a valid actual rear footprint plus race terrain/owner context. Five departing cosmetic volumes grow from the copied source edge band, retain real terrain support as they move outward, then contract into a short low aftermath and expire. The frozen component still has a mound-like-core risk. This is a bounded trial for native review, not visual acceptance or a concept match.

## Exact admission

- `FROZEN-MANIFEST.json`: SHA256 `d18532478337f4f7b75300fc3c83ad682dff5d38bb77a80dc5f612a58d89acde`, 6,607 bytes.
- `v30-contact-volume.patch`: SHA256 `4841cfa1a549437c0795cd06afb83e9e909694c411847862262962a5e00fd426`, 35,363 bytes.
- Four source files: GalacticEffectsView, SolidEffectGeometry, SolidEffectMaterial and the single existing GameApp contact call's `{ terrain, owner, sequence }` argument.
- Nine existing tests change only the explicit owned-child count 8→9. Their exact originals are in `before/`. One new test file contains five meaningful cases (hero/rival count separately).
- All 14 bases were checked byte-for-byte against live source before freeze; the new test target was absent. `git apply --check --whitespace=error-all` passed (empty `apply-check.log`, exit0). Root alone centrally applies. No live source, public geometry, camera, rig, heat, simulation or network edits were made by this work.

## Why the V29 streak was weak

`diagnosis.json` uses one actual hero source fixture with native-equivalent brake/boost onset. At fixed frame922 the reconstructed vehicle XYZ matches the raw native sample exactly (0m difference). The first contact birth is frame922, .083333s after wreck; the event/capture clock map is approximate, and native render accumulator/camera orientation was not recorded. The previous wrong legacy boost onset and its .160315m mismatch are retained as `initial-legacy-input-*`.

At native debris-return11.72 / closest sample971 (FXage .408333), the surviving secondary sheet is only .598m high, viewed at face cosine .047913. Its geometric solid angle is .003928sr; only 3.922% of a uniform 17×9 quad sample grid lies below rendered terrain. Thus terrain burial does not explain most of this sheet's weak visibility: it is nearly edge-on. The primary sheet is .16m high, opacity .16318, and 48.366% of that sample grid is below terrain. At settled12.52 / closest sample1067, FXage1.208333, both old sheets have expired. Physical/beauty-terrain interpolation differences in this probe are at most .03503m, with local course offset0. These are geometric source measurements, not exact native pixels, shader-visible coverage, or casing occlusion measurements. The original V29 5/10 blind critic and all actual media remain untouched.

## New bounded renderer

- Five world lobes: three use source edge1 along the copied band, two use source edge0. The original measured rear contact remains the only witness. Copied edge projections and departing dust never claim new casing collisions or updated contact bands.
- Each lobe is one fixed geometry containing four overlapping closed rounded subcaps, 136 vertices and 256 triangles. Source bounds fit ±1.06 local XZ and [0,1] Y, asserted against every source vertex. Total 1,280 triangles for one rear contact, 10,240 at all40 slots. The first 144-triangle smooth trial and the craggy 256-triangle cap trial are preserved.
- One owned instanced mesh/material/geometry, fixed40 slots, one160-byte instance-age attribute. Maximum owned effects children becomes9 from8. Existing debris64 / explosion plates16 stay unchanged. In the valid actual-context branch the old two contact sheets are not emitted; all10 existing pieces (3 casing,7 grit) keep their prior behavior. Generic and context-free calls retain their old plates and clocks.
- Height swells over .14s to1.3–1.8m, then settles toward .35–.55m near1.2s and contracts to nothing by1.65s. Cosmetic outward travel is bounded below1.2m. The actual birth band and anchors remain immutable.
- Nine real terrain probes per changing lobe update: center, four cardinal offsets, four corners. A fitted slope plus maximum sampled residual supports the flat bases; probe positions move with the cosmetic volume.45 queries for a single active contact /360 for40; repeated same game clock uses cached matrices with zero terrain calls. No per-frame source geometry scans, geometry writes, iterative support solver or new gameplay impact.
- The nine-probe fit is not continuous-surface collision. Unmeasured terrain between probes can still naturally occlude the volume. This is not a new raised terrain surface. Actual hero/rival full dust vertex checks have minimum physical clearance .039701m/.039529m; maximum base gaps are .080765m/.082063m in the bounded inspected sequence.
- Opaque cores keep depth test/write and participate in the existing opaque prepass. No vertex shader displacement makes depth/mask matrices disagree. World cofactor normals handle fitted terrain shear; broad sun-facing shade and mild age-driven small billows use the existing sun direction. No new ground-shadow caster or camera-weight manipulation is added. A real gameplay GPU measurement must include the extra owner in applicable prepasses and the procedural fragment work, not count only the one beauty draw.
- Owner/sequence invalidation retires only that owner's slots. Birth overwrite, reset, clear, recovery and expiry null retained terrain references. Near-zero smoothstep dimensions are retired before terrain-gradient division (including1.65−1e−10); malformed terrain retires without NaN matrices. Existing gate/dedup and authoritative game clock remain in force. Geometry/material disposal follows the existing owned-child disposal loop.

## Verification and preserved limitations

`billows-focused-compat.log`:72 tests across13 files PASS. These retain contact/rupture root, generic behavior, owner lifetime/pool identity, shader atlas, gate/dedup, full rotated debris floor and resource lifecycle assertions. New actual-source tests cover all dust vertices versus actual terrain at ages0/.058333/.16/.408333/1.208333/1.64, the exact query budget/held-clock behavior, all40 slots, owner replacement/recovery/reset, disposal, finite malformed terrain and the expiry-neighbour regression. `billows-typecheck.log`: strict private TypeScript PASS. Two private loader failures are preserved: directory/index resolution blocked one suite while67 tests passed; fixing the private resolver made all72 pass. No live test assertion was weakened for the loader failure.

Three actual WebGL component attempts and exact command/console/image hashes are recorded in `component-preview-audit.json`:
1. `output/playwright/round35-combat-v30-component/`: smooth stone-like mound failure. Favicon404 and duplicate Three warning preserved; shader/GL errors0. Initial backward age selection means the first attempt's grit is not an exact chronological effect claim.
2. `output/playwright/round35-combat-v30-component-refined/`: stronger scallop but craggy fine shading; rejected as too rocky. Browser errors/warnings0, shader/GL errors0.
3. `output/playwright/round35-combat-v30-component-billows/`: final rounded overlapping subcaps at .058333/.14/.408333/1.208333. More shoulders and lower aftermath, but still mound-like in isolation. Browser errors/warnings0, shader/GL errors0; real WEBGL_lose_context restoration succeeds.

All attempts use the observed V29 debris-return camera position, actual reconstructed rear birth footprint and physical terrain. The look target is chosen to show the isolated component. No vehicle/casing occlusion, native camera orientation, live postprocess, native gameplay or FPS claim follows from these previews. Each browser was closed immediately after capture/context checks and its owned server was terminated. Root reviewed the final image and explicitly authorized freezing this bounded native trial with the remaining visual risk. No more refinements before native results.
