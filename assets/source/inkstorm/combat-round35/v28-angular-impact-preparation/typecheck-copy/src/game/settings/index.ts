/** Versioned comfort, audio, control and binding preferences. */
export * from './schema';
/** Failure-safe browser persistence for the settings document. */
export * from './storage';
/** Persistent, versioned per-vehicle workshop loadouts. */
export * from './workshopStorage';
