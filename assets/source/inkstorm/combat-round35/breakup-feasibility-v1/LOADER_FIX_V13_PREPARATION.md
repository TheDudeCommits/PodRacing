# V12 loader failure and V13 correction

V12 actual video did not show the prepared engine breakup. The independent fresh critic remained 4/10. The recorded bundle and all source/fixture files preceding this correction are preserved; `v12-before-loader-fix/` retains the original helper/test namespace and `actual-source-structure.json` retains the untouched GLB node names.

Cause: the installed Three GLTFLoader normalizes node names through `PropertyBinding.sanitizeNodeName`. The source group `teemto-engine-left.001` becomes `teemto-engine-left001` (right likewise). The strict factory looked up the raw spelling, so it returned null and correctly retained the valid rigid fallback. Earlier fixtures reconstructed raw JSON node names, missing this real integration step. Their geometric clearance results remain algorithm tests, not proof of V12 activation.

Correction: resolve only the exact sanitized expected semantic names. Structural/identity checks remain; no substring, punctuation-insensitive or ambiguous alias search is added. Hero and rival now also pass the **installed GLTFLoader → actual VehicleArtLibrary → ImportedVehiclePresentation** activation test. The tests build an in-memory copy without material/image references to avoid DOM decoding; every original node/hierarchy, primitive geometry/accessor and BIN byte remains unchanged. They prove loaded-name/instance activation and source/reset preservation, not texture decoding or rendered quality. Source GLBs/public assets are untouched.

The existing racer diagnostics now report read-only `breakupAvailable` and `breakupActive`. Native solo-chase analysis requires at least three Teemto samples with both true during the matte/wreck shot, strictly after the event frame and cue age zero. Recovery reset is excluded. Missing diagnostics, unsupported fallback or an inactive observed sample fails the gate. A CPU-only gate check rejects untouched V12 observations under this new requirement; synthetic true/false controls test only assertion behavior and are never written into native receipts.

Validation: 22 targeted tests across the loaded-geometry/breakup and racer lifecycle files PASS; typecheck, harness syntax and diff check PASS. The 26-checkpoint full-vertex terrain and camera checks retain the prior .550/.533 m minimum hero/rival clearance. The first new test run passed but typecheck found tuple index optionality; an explicit two-name tuple fixed that without runtime changes. Final outputs are `loader-fix-tests-final.log` and `loader-fix-typecheck-final.log`. The previous attempt logs remain. A subagent follow-up was blocked by the global thread limit, so this correction was completed locally.

V13 is a prepared source checkpoint awaiting root's combined build/native capture and activation assertion. No new visual acceptance or performance result is claimed here.
