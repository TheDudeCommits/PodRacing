# V15 renderer contact candidate — source frozen, native review pending

V14 remains an executed **4/10 spectacle rejection**. Its complete temporal review, raw critic and original WebM are preserved at [V14 review](../../../../../output/playwright/round35-combat-v14/temporal-review/REVIEW.md). V15 is a bounded renderer response to the clean hovering wreck, not a new simulation collision system or an accepted visual result.

The same Teemto right-engine node now adds 0.88 rad roll and −0.18 rad pitch over 0.20 simulation seconds while the original separation reaches its envelope at 0.12 s. A smooth 0.16 s settling term lowers that engine toward its cached terrain support point as well as lifting it if necessary. Its margin closes from 0.55 m to **0.10m**; the left engine and rigid-family fallback retain their previous conservative support. Descent is capped at 8 m for airborne cases, with no contact cue unless the source support actually reaches the settled margin. Birth, reassembly and direct reset restore exact original transforms. Geometry, material, UV, normals, cockpit/pilot and source assets are unchanged.

The displayed ground witness and horizontal engine direction are reused vectors on the resolved pose. No per-frame vertex read or hierarchy traversal was added. Actual terrain queries remain 118 for hero / 114 for rival for the two engines on the tested assets. Direct reset, a different pose association, and each rigid solve invalidate the contact output. Other families never receive this Teemto-only articulation/contact.

A bounded eight-racer gate registers an actual wreck frame and monotonically increasing crash count; guest events use their host envelope frame. It waits for contact on a later current frame, ignores duplicate packets/renders, and clears with each existing effects reset. Paused rendering and replay do not emit it. The renderer consumes the current resolved pose, not the preceding rendered frame. It never writes back to the race, vehicle, authority, timer or records.

The one contact emits **ten directional sparks and two low sand lobes** from the sampled ground point, into the existing 64-debris / 16-burst pools. The initial 18-fragment rupture now starts within a 0.8–1.5×√severity metre rear cone and moves outward earlier, oriented by the authored engine transform. Fire/smoke shader and existing flash dimensions are unchanged. No meshes, materials, draw slots, shaders, timers or pool capacity were added: eight draws remain the bound. A single new contact can add 124 active triangles within those fixed pools. Overlapping wrecks can replace older cosmetic slots; no performance acceptance follows from the bound.

The only camera-related addition is read-only `snapshot.game.combatCamera`: `cut`, `recoveryArmed`, `wreckChase`, `wreckRecovery`. Camera behavior and source camera code remain byte-identical to V14. Parent-owned native network checks will use these scalar copies rather than infer a rig from the mode label.

[CPU audit](close-contact-cpu-audit.json) passes 16 breakup/projection tests. The right engine has 6,082,615 full POSITION terrain checks across 259 native-trajectory ticks per hero/rival: minimum 0.099999999999996 m, settled maxima≈0.10m, no burial. Contact is available on 197 sampled frames 930–1126; its witness is≈0.10m from an actual source vertex. A 20 m elevated-pose probe descends exactly 8 m and advertises no ground contact. The rival is a geometric substitution on the same trajectory, not a separately driven native session.

Four-family/source camera coverage passes 78,450,400 snapshot and 8,473,568 dynamic POSITION projections plus 1,088 sequential corner updates. This proves containment for the tested poses; portrait post-birth held widths remain 63.69–64.32%, below the 65% composition target. Existing normal/replay/fallback isolation tests remain in place. FX/gate/controller/simulation-boundary tests pass 40; event-adapter/rigid-pose tests pass 7. **63 unique targeted tests across 8 files, final typecheck and scoped diff-check PASS.** Initial 0.55 m tests and every later result remain separate under this folder.

[Source freeze inventory](source-freeze-inventory.json), [exact V14→V15 patch](v15-ground-contact.patch), `before/` and `frozen/` pin all changes. Native V15 footage must still establish a convincing ground strike, linked rupture/dust, clear silhouette and recovery. CPU support evidence does not establish those perceptual outcomes, continuous motion or FPS. The short contact uses sampled terrain; it is a presentation effect, not authoritative collision truth. No browser, GPU, build or source asset authoring was performed by this task.
