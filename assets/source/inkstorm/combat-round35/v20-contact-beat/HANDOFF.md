# V20 private contact beat — ready for native review

Prepared only. The five-file `v20-contact-beat.patch` has not been applied to runtime. It preserves the V19 recovery cutback and all intact/damaged GLB bytes. No GameApp, simulation, public assets, dist, browser or Blender changes were made.

The authored victim shot chooses its tear-side world azimuth once at the controller’s rising cut edge and retains it through protected wreck recovery. A moving fracture witness crossing the moving assembly centroid can no longer reverse that choice. Existing source-box distance fitting and terrain eye clearance remain active. Ordinary/manual/replay paths clear the cache; other families retain their existing fallback.

The right rear machinery reaches real terrain-supported contact earlier. In the installed hero and rival fixture, contact moved from simulation frame 927/age 0.141667s to frame 919/age 0.075s. Under the existing 0.18 pacing this schedules contact at 416.67ms instead of 787.04ms, within the 820ms held beat. These are CPU timing calculations, not measured video timing. Every rear source POSITION at first contact remains above terrain: minimum hero 0.096704m and rival 0.10m. Downward 8m bound, exact birth/reset, original pilot, cached supports and source normal/geometry assets remain unchanged.

The existing two contact plates become a short low dark sand scrape and a taller delayed sand fan, both travelling from the actual support witness in the existing trailing direction. The existing ten contact sparks become wider while remaining streaks. Unrelated crash/EMP/shield/recovery sparks retain their prior widths. This is transient billboard sand, not terrain deformation or a persistent scar. There are still 8 owned scene children, 16 plate slots and 64 debris slots; no new draw, program, geometry, texture or per-frame traversal is introduced.

Validation: 50 focused tests, 4 full admitted hero/rival geometry tests, 28 camera tests, private TypeScript and `git apply --check` PASS. The geometry suite retained exact no-burial tolerance −1e−6m, every visible POSITION checks across frames 910–1168, source-box/corner projection at four aspects, birth/random-access/reset/pilot checks and 296/222 maximum cached-support terrain-query budgets. Camera regression includes all four registered family fixtures and narrow aspects. Two initial failures are preserved: the old >20 spark aspect and identical 0.48 dust-aspect expectations contradicted the intentionally changed visual shapes; updated tests assert minimum readable width, streak ratio >10 and distinct 0.18/0.54 plate aspects while keeping lifetime, direction, opacity, count and reset checks.

Native acceptance is pending. Judge whether the earlier contact and stable view make the sand/sparks readable, whether the held azimuth exposes the tear as pieces move, and whether protected recovery still returns cleanly. CPU containment does not establish spectacle quality, scenery occlusion, high-speed camera composition or FPS. The earlier V18 fresh critic 4.5/10 FAIL remains the perceptual baseline. No fresh critic has seen V20.

Apply only after checking `candidate-inputs.json` against live source. `contact-timing-receipt.json` records measured geometry/timing. `focused-final.log`, `actual-geometry.log`, `camera-regression.log` and `typecheck-final.log` retain validation. The private Vite resolver runs test-only candidate modules without altering source; `candidate/` contains portable raw files, while `test-runtime/` contains equivalent test copies with absolute imports.
