# V29 low contact consequence — private candidate

One source file and two focused test files; no live source/public writes. Root owns admission, native capture and performance. Camera, rig, game state, fracture heat roots and contact provider are untouched.

## Cause preserved before implementation

The V28 actual debris-return image (`output/playwright/round35-combat-v28/temporal-review/debris-return-pts11.48.png`) shows an upright tan strip. The settled image (`settled-pts12.28.png`) has little remaining ground consequence. The independent original `diagnosis.json` uses one existing actual hero fixture bind, five explicit source poses and the recorded V28 eye position only; it does not reconstruct native orientation or pixels.

At source first contact age .075 s the copied band is 3.054 m long. The old long-lived plate measures 2.454 × 2.831 m at effect age .05, 2.720 × 3.614 m at .30 and 3.145 × 4.866 m at .70. Its absolute face-normal cosine to that eye is .80–.84 (the other plate .88–.89). Thus its tall aspect and vertically narrowing kind-5 mask explain the strip better than an edge-on-plane hypothesis. The one-shot effect copies the early narrow band; later broader engine support does not update it. Preserve the original diagnostic JSON/runner and logs unchanged.

## Structural change

- The same two copied casing-edge roots emit outward-leaning sand sheets. The near edge stays at the existing root + .08 m; no new anchor or contact witness is created. A short rise peaks at 1.30 m for the primary and .77 m for the secondary, then lowers to .16/.12 m. The full cosmetic width expands from the birth band by at most 3.6/5.2 m. This expansion does not change the measured footprint.
- The same ten fragment entries become three dark raking casing pieces and seven ochre clods. They use the existing bent primitive, bounded horizontal drag and existing lifetime. Full rotated primitive bounds clamp above each copied birth plane by .04 m. Solids retain value and contract late instead of acting as luminous needles.
- Two shader kinds (10/11) supply unequal rolling heads and gaps; ordinary kinds 0–9 retain their branches. Packed fragment kind 2 uses the existing world-sun facet response; existing 0/1 response is unchanged. No opacity increase: the existing .82 cap/fade remains.
- Eight existing mesh children, 64 debris slots, 16 plate slots, same ten/two slot writes, unchanged .28–.48 s fragment and .52/1.15 s plate lifetimes (secondary starts +.04 s), depth testing and game-clock behavior. No added terrain queries, geometry scans, allocations per update, texture or draw pool. This does not establish unchanged frame time or fill cost.

## Verification and exact limits

`final-focused.log`: 9 tests / 2 files PASS. Actual hero and rival first-contact fixtures verify fixed roots/copied input, low expanding sheet matrices, full existing fragment primitive floor clearance, normalized basis with near-unit axis and parallel departure, pool bounds, repeated-time determinism, expiry and pool reuse. `final-compat.log`: 47 unchanged tests / 7 files PASS through the candidate source remap. `final-typecheck.log`: strict private source + fixture/tests TypeScript PASS. No browser, build, WebGL shader execution, media decode or gameplay event injection was performed.

`contact-metrics.json`: actual hero/rival minimum primitive gaps .039999730/.039999503 m to sampled planes; maximum rise 1.299999952 m; width at effect age 1.0 is 8.248118/8.262890 m and rise .119999997 m. Float32 world placement near x=18600 is checked to .002 m root tolerance. These are source matrix measurements, not native pixels or visual acceptance.

The original upright-kind/center-drift expectations failed and are retained in `initial-v28-effects-contracts.log`. Their replacement checks keep measured roots, depth, capacity, axis and geometry-floor safety while testing the authorized new low-sheet motion. `initial-wrap-timing.log` preserves a new-test assumption of 16 simultaneously visible generic plates versus actual 11 with staggered births; the final test requires exact clean-history count/matrix/attribute parity and fixed 16-slot allocation. `initial-wrap-field-name.log` preserves a subsequent test-only incorrect private slot field name, corrected to the existing explosionSlots. Neither correction changed source behavior.

Support is against copied horizontal birth planes only. Terrain under outward ejecta is unmeasured and can occlude it naturally; clods born along the true contact band may initially be hidden by the casing. The casing-edge sheet roots are cosmetic terrain projections from the existing provider, not proof of metal contact at outer elevated vertices. Native review must establish a visible broad impulse and low aftermath without a new curtain, excessive coverage or apparent hovering. No visual score, FPS improvement or completion claim is made.

## Admission

`base-hashes.json` and `candidate-hashes.json` identify all three targets. `v29-low-contact-consequence.patch` contains only those targets. Apply centrally only after matching live base hashes; `apply-check.log` records a read-only check. Candidate source and tests are frozen after the final checks. V28 evidence and baseline remain intact.
