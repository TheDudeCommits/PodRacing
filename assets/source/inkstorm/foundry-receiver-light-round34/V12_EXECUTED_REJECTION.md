# V12 executed rejection audit

V12 is rejected. The separate critic preferred V10 in all four matched Foundry pairs, with Medium / Medium / Low / Medium confidence. Both treatments scored 6/10 or 6.5/10 and failed the 8/10 target. The critic described broader cyan spill as weakening panel boundaries and recess legibility; it did not establish light leaking through geometry. These are attributed critic findings: this auditor did not visually view the images.

All ten original PNGs and their ten anonymous attachments match their inventory SHA-256 values and one another. The prompt hash and CLI attachment order also match the invocation. The complete raw event file contains no tool or retrieval items, one final agent message, and one completed turn; return code is 0. Four reconnect events precede an explicit WebSocket-to-HTTPS fallback. The final message matches `review.md` exactly. The isolated ephemeral invocation and trace support an image-only review process; they cannot establish that generic user configuration was absent.

The eleven matched rows were recomputed against both saved receipt files. All six pose fields, draw calls, triangle counts and renderer dimensions/pixel ratio match in every row. Decoding the PNGs independently reproduces all changed-pixel counts and mean absolute RGB deltas. The six non-Foundry views—grid, salt run, canyon, fork, launch and finish—are byte-identical across V10 and V12. Five Foundry views contain pixel changes. Image equality establishes those captured views only.

Startup resource baselines differ. These counters establish neither a +1-program delta nor net-zero resources, a resource regression, or its absence. The V12 receipt records 481 short live samples averaging 60.00274441 FPS, with no captured errors; this is not a full-race or mixed-combat performance result. The saved verification log records 678 tests in 120 files passing plus typecheck/build; this auditor did not rerun them.

The rollback receipt verifies the two restored source files against both the before-receipt and preserved before copies. Both current source hashes also matched that checkpoint when this audit ran. The newly introduced helper is recorded removed, and the historical built bundle requires rebuilding after rollback. These are checkpoint findings, not acceptance of a current V13 build. Browser closure and the empty port are recorded by the runtime owner's matched report and were not independently rechecked here.

The machine-readable audit is `executed-v12-audit.json` (SHA-256 `66740342ea6ab29685800aaf423615534c3f9c53bea5baa2ef5422cceaec8e44`). The verified matched report SHA-256 is `311b83cd27933bc40730ea79c84817c0e4d9755ae1a03cf7fe1356b42edf14a5`. All original evidence was preserved; only this note and the audit JSON were added.
