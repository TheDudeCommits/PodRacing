# Foundry service gantry V2 — substantial source construction pass

Status: root executed source-only Blender MCP authoring, native export and three technical renders successfully. Actual native validation, original-source comparison and separate exact-retained packaging are complete. The packaged derivative passes clean Khronos and final geometry/attribute checks. This establishes source technical readiness; root owns runtime installation and actual-world visual/performance acceptance. V1's strict critic failed source 6.3, local gameplay 5.7 and full-scene 4.8. This pass responds to the actual near-span image, concept11 and original06. Root executes Blender, installs candidates and judges new actual-world images. No V1 or original source file is replaced.

The new overhead pipe will form a continuous inverted U: two 8.3-unit-diameter risers centered on the original X±46 columns descend to the original base plates, while radius7 elbows meet the lowered horizontal center at Z56.1. The principal flange outer radius5.3 establishes top61.4, below V1's61.45; the pipe itself ends at60.25. This replaces the visible blind terminations at beam height. Thick paired metal flanges, an exposed dark gasket and eight substantial bolt/nut pairs articulate the elbows, riser elevations and central repair. The original structural truss, columns, feet and their source attributes remain retained.

Four bracketed annular column landings (two per side), a front ladder on each riser and a wider upper catwalk establish an accessible route from base to pipe. The platforms and all low riser hardware stay outside X±40, within the existing support footprint. Guardrails leave intentional ladder access gaps. Substantial splayed steel knee plates, base gussets, anchor pads and readable bolts give the retained supports a stronger assembly hierarchy.

A larger offset hoist uses a supported I-rail, wheels, cheek plates, ribbed motor, cable drum, double cable legs, a wide lower pulley and an open hook. Its lowest point must remain above39.60 inside the road opening. Caged warm-color fixtures accompany the landings and upper catwalk. These are opaque vertex-color lamp objects in the original one-draw material; this source task does not add real lights, shader emission or runtime behavior.

The new pressure shell receives a distributed, bounded set of facet-projected paint losses and weld collars. New details use the original orange/cobalt/steel/ivory hierarchy, with a warmer lamp lens color. No runtime textures or new material draw are required. The budget is20,000 triangles for the whole candidate; measured count and construction closure will be reported after arithmetic preflight, without using that budget as a visual acceptance proxy.

Hard checks: preserve original 3,940 non-signal triangles and oriented source attributes, remove the same2,360 signal triangles, keep X[-52,52]/Y[-7,7]/Z[-25,61.45] bounds, maintain clipped central corridor minimum≥39.60, retain original physical support footprints, one mesh/primitive/material, no textures/animation. Low risers/landings are permitted only outside the open corridor. Both MCP stages must restore every pre-existing scene/collection membership and shared selection/context, with no shared blend save. Float custom-normal storage remains in use; final native GLB attributes will be checked against original bytes and packaged separately if needed, following the actual V1 importer evidence.

Prepared paths: `author-service-gantry-mcp-safe.py`, `export-render-service-gantry-mcp-safe.py`, `validate-preparation.py`. Expected new source outputs: `foundry-service-gantry-v2-native.glb` and versioned neutral approach/rear/side PNGs, only after root authorizes and executes the guarded stages.


## Prepared construction and measured preflight

The full candidate contains **19,956 triangles**: 3,940 original retained triangles plus16,016 new triangles, under the20,000 limit. The three horizontal joints and four riser joints each have two0.76-thick metal flange lips, a0.42-thick exposed dark gasket, and eight through-shanks with full hex heads/nuts. Six caged lamps occupy four column-platform and two upper-catwalk positions. Four annular platforms atZ20/38.5 and the upper catwalk connect through two external ladders, with narrowed2.36-unit entry gaps in the platform guardrails and open end entries in the upper front rail.

The larger hoist's35-unit I-track is supported at the original X−31/−15/+1 crossmembers. It includes four contact wheels, axles, large cheek plates, a long cable drum, motor/cooling fins, cable legs, broad pulley and open hook. Base gusset tips intersect the radius4.15 riser shell, their pads meet the original top foot plate, and lamp mounts overlap their actual decks. These explicit joins were checked during source preparation; the upcoming DCC views must establish visual quality.

CPU checks report all mechanical groups closed and positive-volume, finite coordinates, nondegenerate triangles, and120 deliberately open projected paint chips (360boundary edges). Added bounds are X[-51.84,51.84], Y[-6.855562,6.665562], Z[3.93,61.4]. Combined bounds retain original X±52/Y±7/Z−25 with top61.4. New clipped corridor minimum is39.690144042475325; the retained original pendant keeps the combined minimum39.66999816894531. Low risers and access remain outside the open X±40 corridor. No support moves or collision contract changes are included.

Files:

- `author-service-gantry-mcp-safe.py`: new stage `PodRacing — Foundry service gantry round33 V2`, object/family `foundry-service-gantry-v2`; same original preserved input, fail-closed5-bank signal removal, retained imported evidence checks, float corner normals, explicit20k/bounds/clearance/palette checks, export defaultOFF, complete context/membership restoration.
- `export-render-service-gantry-mcp-safe.py`: requires successful V2 marker, actual triangle property matching mesh≤20k, retained evidence and1material; source-only native export plus3 neutral full-bounds1400×1000CPU technical views. Uses separate disposable render scene and restores source/shared context. No blend save.
- `validate-preparation.py`: local CPU-only AST/arithmetic checker, with `preparation-receipt.json`. Its filesystem/hash code is never sent to MCP. It explicitly distinguishes preparation from actual Blender/export/render/world acceptance.

Run the scripts only through root's coordinated actual Blender MCP, after external source/path collision checks. Both V1 payloads, V1 native/final GLBs, renders and the original predecessor remain unchanged. The same actual native→original attribute comparison remains required after export; V1 proved import quantization of retained colors/normals, so exact imported Blender evidence cannot be presented as original GLB byte identity.

## Reserved warm glass contract

Only `warm_lenses` uses exact linear RGB **(1.0,0.55,0.095)**; this accounts for168triangles across six fixtures. The authoring guard rejects any use of the reserved range on another new surface. The unchanged source vertex-color material renders these as opaque warm glass and does not create lights/emission itself.

Root separately owns the selective runtime response: classify before wear using `r>=0.98`, `0.50<=g<=0.59`, `b<0.12`, and non-stone material. Original warm(1,.61,.16), coral(.8,.225,.1) and cream remain outside that range. This is selective self-light for the lamp glass, with no claim of neighbor illumination, and no added source draw or texture.

Root has now produced three neutral V2 source renders; actual native validation is documented below. No new actual-world screenshot, visual score or FPS improvement is established by this source task. The previous6.3/5.7/4.8FAIL remains the baseline until root obtains fresh evidence against concept11 and unchanged original06.


## Executed V2 native evidence

`mcp-authoring-receipt.json` records19,956triangles, zero retained imported-normal delta, unchanged39.66999816894531clearance, and preservation of all138pre-existing scenes/collections/context. `mcp-export-render-receipt.json` records successful source export and all3neutral1400×1000CPU renders,139then-existing scenes/collections preserved, source evidence and selections restored, temporary review data removed, and shared Cruise/ViewLayer/activeSketchfab_model.001/15selected context restored.

The native `foundry-service-gantry-v2-native.glb` is1,332,836bytes, SHA256 `51ef5f95695af7cbf0f26f095daa9249af6f373fae7b11567372b8bb7d043320`. `native-khronos-validation.json` reports zero errors, warnings, infos or hints. `native-technical-check.json` reports19,956triangles,33,644vertices, one primitive/material, no textures/animation, finite/nonzero normals and maximum length error1.3115896790871773e-7. Bounds are original X±52/Z±7 (glTF), heightY[-25,61.400001525878906]. The168warm-glass triangles preserve the intended float32 tuple. These native checks do not assert original retained attribute byte identity; the separate oriented original-source check decides packaging.


## Final exact-retained V2 handoff

Use **`foundry-service-gantry-v2.glb`**,1,331,516bytes, SHA256 **`58fc12d837039eb73111886b1b91c294516c51fc47a4a0d00200717161fd39ba`**, for root's controlled Foundry-only runtime review. Preserve the native export for provenance rather than installing it.

`native-retained-check.json` established the same DCC round-trip difference seen in V1: all3,940retained oriented triangles and positions are exact, but580normal corners differ from original by at most0.00029999471735209227 and11,820color corners differ by at most0.003992617130279541. All2,360signal triangles are absent and16,016added triangles are identified separately. The zero-delta Blender authoring guard compares to imported vectors; it does not prove identity with the original GLB buffer.

The root-authorized versioned packer restores the original7,336retained vertex records (position/normal/RGB seams and winding) and appends26,268native addition records without changing their attribute bytes or triangles. It preserves the original material JSON. Final totals are **19,956triangles,33,604vertices, one node/mesh/primitive/material, zero textures or animation**. The family/node is`foundry-service-gantry-v2`; material is`Inkstorm_Industrial_Revision_Paint`.

Final evidence:

- `exact-retained-packaging-receipt.json`: direct original-record and native-addition byte/winding proofs; both inputs unchanged.
- `exact-retained-technical-check.json`: a fresh independent decode/oriented check confirms original retained position/normal/RGB bytes exact. All normals are finite/nonzero/unit within1.3115896790871773e-7. Final glTF bounds remain X[-52,52],Y[-25,61.400001525878906],Z[-7,7], with central corridor height39.66999816894531. All168glass triangles preserve the exact intended float32 palette value.
- `exact-retained-khronos-validation.json`: Khronos2.0.0-dev.3.10, zero errors/warnings/infos/hints.
- `preserved-input-and-render-hashes.json`: original predecessor, V1 final/native, V1 author/export/packer and all V2 native renders remain byte-exact; rechecked by the final technical checker.
- `selftest-preserve-retained-gltf-receipt.json`:16bounded synthetic cases pass, including low risers outside the corridor, rejection inside the corridor, source seam/winding restoration and refusal to overwrite outputs. These are tool tests; the adjacent native/final receipts provide actual-artifact evidence.

The V2 packer is a separate file under`v2/`; V1's utility remains unchanged. The new explicit counts/top/floor bounds replace only V1-specific construction constraints. Whole oriented-source matching, signal exclusion, one-material/no-texture contract, all attribute/format guards, low-corridor triangle clipping, source preservation and exclusive-output creation remain enforced.

The3neutral renders depict the native Blender source, whose geometry and all authored additions match the packaged derivative. The small retained attribute restoration has data evidence, while final perceptual appearance remains for root's actual-world capture and fresh critic. No Blender invocation, runtime/public installation, gameplay capture, build or performance test was performed by this packaging task.
