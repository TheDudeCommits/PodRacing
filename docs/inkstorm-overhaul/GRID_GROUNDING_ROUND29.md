# Grid workshop grounding — round29

The pit complexes now sit on their supporting slabs, and each concrete foundation matches its offset source footprint. Three actual close-camera before/after views have zero browser errors. The fresh blind critic retains the correction, but all target criteria fail: dunes still penetrate the first workshop and the remaining construction/material detail is sparse. [Image review](BLIND_GRID_GROUNDING_ROUND29.md).

The pit-complex instance base changes from terrain-center height minus1.5m to plus1.45m, a2.95m lift. The authored1m-thick slab begins at localY0 and overlaps the foundation top at center height plus1.5m by5cm. Its real footprint is localX[-71,79],Z[-32.5,28], so the foundation is now centered at(4,-2.25) with dimensions150×60.5m. The earlier centered150×61m footprint left portions unsupported. Other families, asset bytes, X/Z/yaw/scale, race routes and collision proxies are unchanged. Existing full-placement shadow instances follow the corrected transforms.

Focused validation passes18 tests across6 files, typecheck and diff checks. The new geometry test compares actual foundation corners with the source slab across three yaw/nonuniform-scale cases on a sloped height sampler. That mathematical support check does not establish clear workshop interiors; the actual images demonstrate the unresolved dune intrusion.

The source-state captures are `output/gauntlet/grid-grounding-round29-before/` and `…-after/`. Each contains three PNGs and a receipt with tracked source/asset hashes before and after, no changes during its invocation and an empty error array. `scripts/inkstorm-grid-detail.mjs` renders the real GameApp world/material/shadow/post pipeline through Vite, with temporary diagnostic close cameras and hidden HUD. Each paired camera position, target and FOV compares exactly, anchored to the unchanged terrain. No geometry, material, occluder or effect override is applied. These are not ordinary race-camera or frozen-bundle/FPS images. Both owned browsers and port5195 servers closed.

Next: shared physical/rendered off-road pad grading, with unchanged road/shoulder guards and stable center anchors. Original additive repair equipment, awning thickness and gantry-joint geometry remain a separate source-production phase. Do not conceal the dune problem under new props or claim concept acceptance. The completed [round28 performance result](FULL_RACE_PERFORMANCE_ROUND28.md) predates these source changes; no new performance claim is inherited.

## Shared grading integration

The next source candidate removes the visible dunes from all three workshop interiors in the same three diagnostic cameras. The three building matrices, camera positions, targets and FOV are byte-for-byte equal to the slab-only capture. Terrain, shadow-depth displacement, MRT normals, roads and particle samplers now share the instance-owned pit offset. Rigid pits and foundation tops use the saved ungraded anchors, avoiding a second lift after grading. [CPU proposal and limits](PIT_PAD_ROUND29_PROPOSAL.md).

The flagship field is a330×537 one-metre R32F atlas (708,840bytes), with nearest texture filtering and explicit four-texel bilinear sampling identical to the CPU field. It adds no mesh, draw or render pass. The existing two gulf arrays and signed composition remain unchanged. The pit texture has independent replacement/disposal; clearing a gulf cannot leave or destroy the wrong pit field, and course restart explicitly resets both. Conservative bounds include the actual pit offset extrema. This does not add a dedicated off-road pit-bank caster to the static sun atlas.

The combined focused run passes24 tests across5 files, typecheck and diff checks. New render lifecycle tests cover rectangular texture dimensions, shared uniforms, independent replacement/disposal, sampler agreement, bounds reset and saved foundation anchors. Actual capture `output/gauntlet/grid-grounding-round29-graded/` contains three PNGs and zero browser errors; browser and port5195 closed. The source receipt is `assets/source/inkstorm/pit-pad-round29/render-integration.json`.

These images expose a straight raised apron and steep frontage bank, with still-sparse violet workshop interiors. A fresh independent review is pending. This source has not been built into the frozen round28 bundle and has no round29 timing or target-parity acceptance.

The isolated actual-GLSL GPU sampler check also passes:175 points in four states (combined, pit-only, gulf-only, cleared), maximum GPU/CPU difference0.00000324082m at identical float32 coordinates. The separate maximum world-coordinate quantization delta is0.000560849m. Every protected-road pit sample is exactly zero. All six lifecycle texture objects dispose once; all four states reset independently. This is shader readback evidence, not an FPS or screenshot-parity test. Receipt:`output/gauntlet/pit-sampler-round29-gpu/receipt.json`; runner and shader diagnostic are preserved under the pit-pad source folder. Browser and port5197 closed.
