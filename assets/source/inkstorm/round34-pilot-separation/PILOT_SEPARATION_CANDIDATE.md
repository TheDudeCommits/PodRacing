# Blockrunner pilot separation — private candidate, 2026-09-08

Implementation-aware assessment, with prior source and runtime context; **not fresh blind**. I individually viewed the V8 contact-color garage-default, grid and Foundry-middle PNGs and concept12. Exact viewed/input hashes are in [viewed-inputs.json](viewed-inputs.json). The capture receipts identify `index-CFofn-oM.js`, SHA256 `2d8ff97639d335f22e1c683562c418d3f55572251f1b55dc71268bda015d264b`; V8 is the changed source package, not a different JavaScript bundle at this checkpoint.

The pilot is visible. Its torso/back reads as one flat gray block, most strongly in Foundry shade; the helmet is only moderately brighter, and the garage shoulders lose separation against the graphite cockpit wall. Concept12 supports a slate suit below a titanium helmet and dark visor. Its invented geometry/perspective is not a camera or anatomy authority.

The existing shader multiplies the already gray atlas by the cel ramp, then applies shared scenery shade. `TEEMTO_CLOTH_STYLE` contributes no rim and almost no specular. Its `normalStrength: .25` controls a normal map, not geometric normals; the current Blockrunner package has no normal map. Increasing that field would not address this image.

The [private patch](blockrunner-pilot-separation-v1.patch) adds only a finite `[0,1]` `baseColorStrength` field to the exact-name surface-style whitelist, using the existing CelMaterial uniform and shader. Blockrunner suit strength `.86` uses slate bands `#8d99ad / #b3bdc6 / #d4d6d5 / #eee7d8`, matte specular `.035`, pale rim `.025`, and zero reflection. Helmet strength `.94` uses titanium bands `#8b98ac / #b6c0c9 / #e0e1dd / #fff4e3` with its existing specular/rim/reflection. Body, visor, gloves and every other appearance retain their settings.

This intentionally reduces linear atlas contrast to 86% on the suit and 94% on the helmet, including baked contact contrast; the source texture pixels remain exact. It is not emission or a white cloth replacement. A CPU formula sanity check using the documented pre-AO `#606B76` suit swatch predicts the second band `#46515c → #5d6772` in sun, and `#2a2e44 → #393d55` under the exact full-shade multiplier, before highlights/post-processing. The complete [formula table](diffuse-formula-sanity.json) uses illustrative AO factors and is **not a rendered A/B or sampled texture prediction**. Actual V10 capture must decide whether the lift is sufficient and whether contact depth softens too much.

There are no new shader branches, passes, texture objects, draws, meshes or per-frame work in this patch. Existing shader code, atlas/roughness bindings, geometry, normals, UVs, LODs, attachment positions, camera and physics are unchanged. Seven prepared test cases cover boundary values, independent leases and failed replacements; the existing exact-material test also verifies precedence, case-sensitive matching and borrowed-map preservation. `git apply --check --whitespace=error` passed before parent application. I ran no tests/build/browser/MCP. Parent subsequently reported applying the patch and starting V10 verification; no executed V10 result is asserted here.

Patch: 9,430 bytes; SHA256 `167fd87636668b3b3183948c4025322db7788e43910e4a96cc8bdccb50488ea9`. Baseline/candidate files and hashes are preserved under this folder. Public V8 hero pin: 4,198,472 bytes, SHA256 `ee744b122b3e2a009ef7a8f3a8c5c5848fe1503b13e4334e0fe8276ce5c44d8d`; rival: 3,341,896 bytes, SHA256 `b680cea9dd832ca6c5af382fed5eeea57e4baf6b7d119f0f8bace3dfa6037cb9`. Neither package was edited by this task.
