# Fresh progress review — 19

Verdict: **PASS for the two scoped code paths, with bounded retention and the acceptance limits below.** No concrete correctness defect found in this independent source review. No agent reports, status documents, or history were read. No browser, build, test suite, or competitive storage was run or mutated; only this report was added.

- **Completed Cup replay:** `RaceMastery.restartChampionship()` clears only `championship.rounds`, persists the profile, and selects `cup-canyon` (`src/game/mastery/RaceMastery.ts:165-169`). Event selection does not erase records/history. Ordered award and duplicate-finish guards remain in place (`:204-218`, `:79-80`, `:145-148`). A slower new-series run does not replace a matching PB (`:113-121`).
- **Reachable controls in source:** The results action appears after the completed third round when there is no next event (`src/ui/RaceHud.ts:1206-1225`). The returning garage derives Cup completion from saved round count, independently of the default selected event, and shows the replay action while preparing a race (`:1077-1098`). Both controls use the same delegated action (`:800-812`), whose solo-game route prepares the first round (`src/render/app/GameApp.ts:1731-1733`, `:1528-1539`). Starting a new Cup is explicit; reloading or selecting an earlier round does not silently reset it.
- **Edition changes:** Parsing preserves individually valid old PB identities, times, lap/sector times, medals, and timestamps as archived metadata, while stripping their ghosts (`src/game/mastery/storage.ts:68-74`, `:102-112`). Old-generator saved courses move to the archive (`:114-120`). Existing archive entries are never promoted into active records or events (`:90-100`); the runtime consults active records/favorites only (`src/game/mastery/RaceMastery.ts:45-50`, `:62-64`, `:171-187`). Current comparisons use the full identity, including generator, physics, rules, course/director seeds, class, tune, build and event conditions. A malformed ghost does not discard an otherwise valid PB.
- **Persistence:** Normal save and ghost-quota fallback retain archives, PB metadata, history, courses and preferences (`src/game/mastery/storage.ts:167-173`). Failed durable storage is reported; this is not a guarantee that a blocked/full browser store survives closing the page.

Retention is deliberately finite: 36 active PBs, three active ghosts, 72 archived PBs, 16 active saved courses, 32 archived courses, and the latest 40 history rows (`src/game/mastery/storage.ts:7-10`, `:142-154`). Archive overflow discards oldest metadata; malformed/unsupported profiles or payloads above the parser size limit are rejected (`:84-89`). Therefore “all prior progress forever” would overstate this implementation. Archived metadata has a count notice, not a browsable historical-record screen. Version isolation also depends on incrementing the relevant version constants when competitive conditions change.

The tests read include the completed-Cup reload/replay/slower-run checks (`tests/race/mastery.test.ts:155-190`) and repeated archive migration, invalid entry rejection, deduplication and quota fallback (`tests/race/masteryArchive.test.ts`). They support the logic under controlled fixtures; they were not executed by this reviewer.

`scripts/cup-cycle.ts` provides a meaningful ordinary-input lifecycle harness with clean finishes, raw receipts, persistence comparison and explicit browser cleanup. A bounded follow-up review read only the revised harness: **both replay source paths now have prepared click coverage.** Before replay, the returning garage must preserve completed standings and all non-Cup fields (`:265-266`). Its real button is clicked, and the harness requires one restart action, a prepared `cup-canyon` with zero banked rounds, persisted empty standings, and unchanged non-Cup progress (`:267-280`). The original page then exercises its still-rendered results action and verifies the same fresh-series transition (`:282-294`). The two pages share origin storage but hold separate loaded game state; the second check still requires the results page's completed in-memory Cup to reset. No profile fixture or direct state mutation is introduced by this sequence. This closes the earlier visibility-only coverage gap in the harness source; **browser acceptance remains pending and was not performed by this reviewer.** The optional `--finish-replay-round` flag must be present to claim a new Cup's first round was completed (`:301-311`). The viewport check is 1440×900, not mobile/touch acceptance.

These are source-level findings. They do not establish visual quality, human driving feel, medal balance, fun, production deployment state, or the outcome of a browser run performed elsewhere.
