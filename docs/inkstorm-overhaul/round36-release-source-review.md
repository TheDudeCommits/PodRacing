# Round 36 release source review

Reviewed 2026-09-09T15:48:30.423242+00:00, against HEAD `5925d437f97b57939ffbf02306979d238627491e`. **No new runtime release blocker identified in this bounded source review.** This is independent, implementation-aware review: I previously inspected the preview lifecycle, resource harness and handling evidence. It is not a fresh blind critique. No browser, GPU, simulation run, test suite or build was launched for this review; final full-race acceptance belongs to the root's currently running exact-build checks.

## Retained source behavior

- **Battle and actual pods:** `RaceMastery` defaults to `inkstorm-battle`; its event is chaos/circuit/one lap and the initial `GameApp` simulation uses that selected profile. The default field is eight racers, and solo construction/rebuild assigns all racers the podracer class. The four public appearances are Teemto, Sebulba, Polwo and Blockrunner; a persisted procedural preference normalizes to Teemto. This is four imported appearances on the shared pod physics class, not four newly distinct handling models. Stock Battle excludes saved workshop overrides. Clean Race and Time Trial retain the non-chaos weapon suppression in `RaceSimulation`; Battle's fire/mine/shield inputs reach the existing action path. The quick-setup test exercises real weapon shots, mine deployment and movement of all seven opponents, rather than checking labels only; it was read, not rerun here.
- **Shared marker placement:** `RaceSimulation.routeMarkers` is the authoritative filtered list passed to both `GameApp` course-view construction paths. Placement checks other main/ground-branch lanes, low bridge overlap and actual obstacle proxies. Rendered cone radius.62m, bottom−.2m and light top4.9m agree with collision values. The renderer's optional standalone fallback exists for callers without a simulation list; shipping GameApp supplies the actual list. This removes the prior separate render/collision stride mismatch without making markers nonphysical. The new tests compare Float32 instance positions to collision positions and inspect five production seeds.
- **Hover rollback:** The complete `podracer.ts` diff against HEAD changes only collision yaw impulse conversion into the craft-local basis. Suspension, gravity, landing damping/damage and repulsor envelope are unchanged; `config.ts` is byte-identical to HEAD. The retained drive-version bump remains appropriate for changed collision behavior. No road/grading patch was applied.
- **Preview ownership/cache:** Refresh now prepares and awaits the hero mesh even when its image is cached, checks lifecycle/resource identity after that await, and only reuses image bytes when active appearance and status match. On-demand inspection uses a coalesced RAF and2D display copy, not a perpetual second render loop. Hide/dispose cancels inspection scheduling and releases GPU resources. Prior same-input native failure and its corrected pass remain separate; the resource check passed on its explicitly recorded earlier bundle. Those receipts are not silently transferred as final-bundle visual/FPS acceptance.

## Current documentation reread

Root corrected the three front doors during review. A second read of `ROUND36_REPORT.md`, `HANDOVER.md` and `STATUS.md` finds no remaining claim that relative-hover suspension or slope-relative landings are accepted shipping behavior. They now describe the retained marker/yaw changes, withdrawn experiment and preserved failures. `HANDLING_ROUND36.md` and both historical handling-review banners agree. The earlier resource counts are explicitly labeled as a preceding-build check awaiting a final-build rerun.

The root reports the final suite as961/164 passed and the new bundle as `index-DH3wvA-l.js`; this source review did not independently run that suite or validate its bundle receipt. Front-door final metrics correctly remain pending until completed exact-build race/resource receipts are available. Preserve that boundary when publishing. The gallery's menu stills and earlier native controls are narrower visual/input evidence; they do not establish performance for the restored-hover runtime.

## Limits

No new visual readability verdict, complete leak-proof claim, online acceptance, controller feel or full-race/performance result is granted here. Existing source guards/tests and previous exact-build observations support the narrower conclusions above. Network setup and failure timing were not exhaustively reviewed. Broader fleet/world/crash-art acceptance remains separate.

Reviewed source pins: `podracer.ts` `36682ebe5a45da3c3c55d874d296f58caee3c2492d72a34068dddbab1fc1dc5b`; `GameApp.ts` `9df9fb3cde7b2df3036facde691969be025e137ea3337ed563895ded77ac3c83`; `VehicleCardPreview.ts` `19cefb9a845e57c29f915b51cfbeb41422582dfd81949fecb30fa0fa5240e1a6`; `courseMarkers.ts` `374e7a31c327952e396f6be690ea476d176f99c2fa54d3290a79042f8683fe78`.
