# V7 short contact-color trial — source only

The narrow candidate multiplies the preserved V4B pigment/wear output by `0.62 + 0.38 × AO`, with AO distance **0.16 original source-world units**, 16 node samples, `inside=False`, `only_local=False`, and the Normal input left unlinked. Unoccluded color stays unchanged; fully occluded color can darken by at most 38%. Both copied owners remain present so pilot/body contacts can contribute. This is an offline base-color candidate, with no directional lamp term and no runtime lighting or performance claim. Concept12 supplies art direction only.

Parent executed [15d](../mcp-safe/15d-author-contact-v7-composed-payload.py) successfully in **Blender5.2.0 LTS**. The [actual author receipt](../mcp-safe/contact-author-v7-receipt.json) records new scene `PodRacing — Blockrunner short contact color round34 V7`, two meshes/44,028 triangles, body3/pilot4 slots, and all152 preexisting scenes/collections/context preserved (153 scenes afterward). Original source, fitted source, V4B geometry/material signatures, exact corner normals, both authoring masks, seven extended material graphs and four packed image records passed preservation checks. The V7 author step performed no bake, render, export, normalization or shared `.blend` save.

The copy changes seven color branches: each former `Inkstorm Paint Color` node is renamed `Inkstorm Preserved Pigment Wear Color V7`; a new AO node feeds a multiply node named **`Inkstorm Paint Color`**. Thus stage10's explicit `nodes[CHANNEL_NODE].outputs[0]` path reaches the AO-composited output. Each complete original graph is reconstructed in a comparison record after accounting for these edits and must match exactly. Roughness input ancestry is checked separately and remains exact. Material slots are replaced in place; no clear/reappend, unwrap, normal encoding or topology edit occurs.

Seven disconnected old **color** bake-target nodes are removed only on the new materials. The seven disconnected roughness targets remain, referencing the same two existing masters. No image is created or repainted by authoring. The later finalizer must distinguish two new V7 color bakes from **reused V4B roughness**; it must not manufacture V7 roughness-bake receipts.

## Inputs and guards

- Parent: [actual V4B final bake state](../mcp-safe/bake-v4b-pilot-roughness-receipt.json), [V4B author lineage](../mcp-safe/paint-author-v4b-receipt.json), original source/fit receipts.
- Four parent images: exact metadata/packed-byte FNV from the successful [V6 clamp receipt](../mcp-safe/atlas-clamp-v6-receipt.json), verified against unchanged external PNG bytes while composing. Roughness master receipt hashes and image identities are carried in the V7 author receipt.
- Safe extended helper: [15d-ao-contact-guards.py](../mcp-safe/15d-ao-contact-guards.py), function `contact_graph_signatures(scene)`. It records explicit operations and relevant shader properties, all input/output defaults and identifier links, active bake node and image mappings. This supplements the legacy source signature, which omits AO properties.
- [Latest author preparation inventory](preparation-inventory-v4.json) pins receipts, helpers, PNG files and the111,072-byte safe payload. Its filesystem/SHA composer is external only and must never be pasted into MCP.

The initial static reviewer found no formula, material-slot or graph-inversion problem, and identified two transaction weaknesses. The revised author nests rollback under `finally` even when context restoration raises and declares success only after preservation checks. Two actual safe-mode rejections occurred **before execution**: [module-as-value preflight](../mcp-safe/contact-v7-attempt1-safe-mode-rejection.json), then [computed attribute lookup](../mcp-safe/contact-v7-attempt2-safe-mode-rejection.json). Versions15/15b/15c and their inventories remain preserved. Actual15d uses direct/literal property access and retained all semantic AO/roughness/source guards.

## Prepared next operation

[Matched driver render payload](../mcp-safe/15-contact-v7-driver-render-composed-payload.py) is87,953 UTF-8 bytes, SHA256 `3567b57d8b272e545a4caf7b000a07d736811411cfe9b79dfa7a5691bd229722`. It is composed from the **actual V7 author receipt** and [actual V6 driver camera receipt](../mcp-safe/atlas-clamp-v6-driver-render-receipt.json). Camera, lights, Standard transform, 1280×960 output and16 Cycles samples match. AO retains16 samples. Only a disposable two-owner render scene is created, with no floor or extra occluding geometry; stored V7 settings stay untouched. Exact V7 and V4B graph/image/mask/normal guards supplement source/fit/context checks. Temporary materials and meshes are compared against the source and removed afterward. [Render preparation inventory](../mcp-safe/15-contact-v7-driver-render-composed-payload.preparation.json) pins its inputs. This report does not claim that render has executed.

After actual visual review, two color-only EMIT bakes can use the existing stage10 owner selection while retaining the other owner as an occluder. Add explicit before/after contact-graph guards; the new bake image/active-node change must be accounted for separately from the preserved shading graph. Actual AO bake output must be checked because Cycles scene samples are1 in the existing bake fragment while the AO node is16. Keep the two roughness masters unchanged and carry honest reuse lineage into a new atlas copy. Apply EXTEND/ClampToEdge to final atlas nodes, retaining the successful V6 seam correction. Review contact detail at eventual1024/512 hero and512/256 rival atlas resolutions before any public replacement.

This term addresses local contacts, stud bases and recesses. It does not itself add surface structure to unoccluded red planes or establish good in-game response. Pilot leg/seat silhouettes, nozzle openings and source geometry remain unchanged. Material acceptance, any new critic result, image/bake fidelity and runtime admission remain pending.

Primary documentation supplied and checked by parent: [Blender AO node manual](https://docs.blender.org/manual/en/latest/render/shader_nodes/input/ao.html) and [ShaderNodeAmbientOcclusion API](https://docs.blender.org/api/current/bpy.types.ShaderNodeAmbientOcclusion.html). Actual node/socket/property construction in5.2.0LTS, not the latest documentation alone, establishes the authoring compatibility recorded above.
